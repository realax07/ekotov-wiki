# TC-form-004 — Создание нового значения в поле «Теги» формы (в отличие от поиска)

- **CHK:** CHK-115
- **Change:** add-r2-categories-settings
- **Источник:** tasks: Автодополнение (FR-26) / Scenario: Создание нового значения, отсутствующего в подсказках
- **Тип:** позитивный | **Приоритет:** Should
- **Среда/предусловия:** поднятый стенд (`GET {BASE_URL}/api/health` → 200); Playwright (Python): `page`, `expect`; выполнен вход owner (логин `owner`, пароль `QaOwner_Pass_1!` — seed, как TC-UI-002 чеклиста e2e-critical-path); seed справочника выполнен (CHK-139 / TC-env-001).
- **Сценарий:**
  - **GIVEN** значения `QAT-newtag` нет среди подсказок (ни на одной существующей задаче)
  - **WHEN** пользователь вводит `QAT-newtag` в поле «Теги» и сохраняет задачу
  - **THEN** задача сохраняется с тегом `QAT-newtag` — в ФОРМЕ новое значение допустимо (в отличие от поиска, FR-17; эскалация в — по доменам)
- **Шаги:**
  1. Контроль: `GET /api/suggestions` → `QAT-newtag` отсутствует.
  2. Открыть форму задачи: название `QAT-новый-тег`, в поле «Теги» ввести `QAT-newtag` (без выбора из подсказок) → сохранить; форма закрывается без ошибки ввода.
  3. `GET "/api/search?archived=false"` → найти задачу, проверить `tags`.
  4. Cleanup: DELETE задачи.
- **Ожидаемый результат:** шаг 2 — создание принято (свободный ввод в поле тегов формы легитимен); шаг 3 — задача с тегом `QAT-newtag` создана. Противоположная семантика поля «Категория» (select, CHK-82) не затрагивается — кейс только про «Теги».
- **Тестовые данные:** тег `QAT-newtag`; задача `QAT-новый-тег`.
