# REVALIDATE UPDATE — TC-UI-009 (CHK-144)

> **REVALIDATE UPDATE.** Не новый кейс: файл-обновление существующего кейса `TC-UI-009` (`tests/web/test_board_tasks_ui.py::test_all_attributes_create_view_edit`; кейс — `test-model/approved/e2e-critical-path/ui-01.md` §TC-ui-009). Источник: impact §2.1 (revalidate, pending_update); пункт чеклиста CHK-144. Обновление теста — qa_automation через qa_case_reviewer (G8).

- **CHK:** CHK-144
- **Change:** add-r2-categories-settings
- **Затрагиваемый кейс:** TC-UI-009 «Все признаки задачи: создание → карточка → редактирование»
- **Причина обновления (подтверждено кодом — impact §2.1):** (а) ассерты текста приоритета `get_by_text("high"/"medium", exact=True)` в карточке и бейджа на доске упадут с локализованными бейджами «Высокий/Средний» (FR-29); (б) `get_by_label("Категория").fill("Работа")` упадет на select (FR-19/FR-30).

## Таблица «было → станет»

| # | Шаг/ассерт кейса (было) | Станет (после обновления) |
|---|---|---|
| 1 | Создание: `page.get_by_label("Приоритет").select_option("high")` | Без изменений (select option по value) |
| 2 | Создание: `page.get_by_label("Категория").fill("Работа")` | **ЗАМЕНИТЬ:** `page.get_by_label("Категория").select_option("Работа")` — выбор из select справочника (категория «Работа» есть в seed — CHK-139/TC-env-001) |
| 3 | Создание: `.fill("Дом")` в шаге 1 (создание с категорией «Дом») | Аналогично: `select_option("Дом")` («Дом» в seed) |
| 4 | Просмотр карточки: `#task-detail-attrs` содержит `get_by_text("high", exact=True)` | **ЗАМЕНИТЬ:** карточка содержит локализованный бейдж `get_by_text("Высокий", exact=True)` (FR-29: бейджи приоритетов локализованы; фактическое значение value=high проверять select'ом формы, бейджем — на карточке) |
| 5 | Редактирование: `select_option("medium")` + `get_by_label("Категория").fill("Работа")` | `select_option("medium")` — без изменений; категория — `select_option("Работа")` |
| 6 | Проверка новых значений: `#task-detail-attrs` содержит `medium`, НЕ содержит `high` | «medium» → **«Средний»** (локализованный); «high» → «Высокий»; семантика «новые видны, старые исчезли» сохраняется |
| 7 | Бейдж на карточке доски: `get_by_text("medium", exact=True)` | **ЗАМЕНИТЬ:** `get_by_text("Средний", exact=True)` (локализованный бейдж, FR-29) |

## Критерий приемки обновления

- Ни одного `.fill()` на поле категории в тесте (только `select_option`); все текстовые ассерты приоритетов заменены на локализованные бейджи («Высокий»/«Средний»).
- Категории, используемые тестом (`Дом`, `Работа`), гарантированы seed'ом (CHK-139) — зависимость прописана в фикстуре/комментарии.

## Ожидаемый результат обновленного кейса (GIVEN/WHEN/THEN)

- **GIVEN** пользователь авторизован; справочник содержит `Дом`, `Работа` (seed)
- **WHEN** создается задача со всеми признаками (категория — select, приоритет — select), просматривается карточка, признаки редактируются
- **THEN** все признаки видны введенными значениями; бейджи приоритетов локализованы («Высокий»/«Средний»); категории выбираются только из справочника; после редактирования новые значения видны, старые исчезли
