# TC-form-003 — Выбор существующего значения из подсказок формы

- **CHK:** CHK-114
- **Change:** add-r2-categories-settings
- **Источник:** tasks: Автодополнение (FR-26) / Scenario: Выбор существующего значения
- **Тип:** позитивный | **Приоритет:** Should
- **Среда/предусловия:** поднятый стенд (`GET {BASE_URL}/api/health` → 200); Playwright (Python): `page`, `expect`; выполнен вход owner (логин `owner`, пароль `QaOwner_Pass_1!` — seed, как TC-UI-002 чеклиста e2e-critical-path); seed справочника выполнен (CHK-139 / TC-env-001).
- **Сценарий:**
  - **GIVEN** подсказки содержат значение `QAT-выбор` (заведено тегом существующей задачи)
  - **WHEN** пользователь выбирает `QAT-выбор` из подсказок
  - **THEN** значение добавляется как тег создаваемой задачи
- **Шаги:**
  1. Создать задачу-источник: `POST /api/tasks` `{"title":"QAT-источник-хинта","tags":["QAT-выбор"]}` → 201.
  2. Открыть форму задачи; в поле «Теги» ввести `QAT-выбор` и выбрать значение из подсказки datalist (клавиатура: стрелка вниз + Enter, либо клик по опции — зафиксировать фактический механизм).
  3. Заполнить название `QAT-задача-с-выбором`, сохранить; дождаться закрытия формы.
  4. `GET "/api/search?archived=false"` → найти `QAT-задача-с-выбором`, проверить ее `tags`.
  5. Cleanup: DELETE обеих задач.
- **Ожидаемый результат:** шаг 4 — задача `QAT-задача-с-выбором` имеет тег `QAT-выбор` — выбранное из подсказок значение сохранилось как тег (FR-26).
- **Тестовые данные:** тег `QAT-выбор`; задачи `QAT-источник-хинта`, `QAT-задача-с-выбором`.
