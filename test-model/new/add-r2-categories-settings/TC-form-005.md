# TC-form-005 — Негативный: подсказки формы требуют авторизации

- **CHK:** CHK-116
- **Change:** add-r2-categories-settings
- **Источник:** tasks: Автодополнение (FR-26, следствие NFR-7) / Scenario: Негативный: подсказки формы требуют авторизации
- **Тип:** негативный | **Приоритет:** Should
- **Среда/предусловия:** поднятый стенд (`GET {BASE_URL}/api/health` → 200; `BASE_URL` по умолчанию `http://127.0.0.1:8080`); выполнен вход owner (логин `owner`, пароль `QaOwner_Pass_1!` — seed `tests/api/conftest.py`, куки в `owner_cookies.txt`); seed справочника выполнен (CHK-139 / TC-env-001: `Дом`, `Работа`, `Личное` в справочнике до прогона).
- **Сценарий:**
  - **GIVEN** вызывающий не имеет действующей сессии
  - **WHEN** вызывающий запрашивает подсказки через API
  - **THEN** API отвечает 401; значения не возвращаются
- **Шаги:**
  1. `curl -s -i {BASE_URL}/api/suggestions` (БЕЗ куков) → код и тело.
- **Ожидаемый результат:** HTTP **401**, тело `{"error": "unauthorized"}` — подсказки переиспользуют защищенный эндпоинт (sdd §3.2: `GET /api/suggestions` без изменений, за общим middleware NFR-7); списка значений в теле нет.
- **Тестовые данные:** запрос без куков.
