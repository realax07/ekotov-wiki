# TC-form-011 — Анимация открытия и закрытия формы

- **CHK:** CHK-122
- **Change:** add-r2-categories-settings
- **Источник:** tasks: Визуал формы (FR-29, Д-5) / Scenario: Анимация открытия и закрытия формы
- **Тип:** позитивный | **Приоритет:** Should
- **Среда/предусловия:** поднятый стенд (`GET {BASE_URL}/api/health` → 200); Playwright (Python): `page`, `expect`; выполнен вход owner (логин `owner`, пароль `QaOwner_Pass_1!` — seed, как TC-UI-002 чеклиста e2e-critical-path); seed справочника выполнен (CHK-139 / TC-env-001).
- **Сценарий:**
  - **GIVEN** пользователь авторизован и находится на доске
  - **WHEN** пользователь открывает и закрывает форму задачи
  - **THEN** окно открывается и закрывается с анимацией в рамках единого визуального стиля формы (Д-5: без выбора стиля Заказчиком — текущий стиль + обязательный минимум)
- **Шаги:**
  1. Открыть `/board`; снять computed style оверлея `#task-form-overlay`: `style_hidden = evaluate("el => getComputedStyle(el).transition + ' | ' + getComputedStyle(el).animation")`.
  2. Открыть форму кнопкой «Создать задачу»; снять computed style видимого состояния; проверить, что заявлены transition/animation (непустые значения, не «all 0s»/«none»).
  3. Наблюдаемость: открыть форму повторно и приложить скриншот в момент ~100 мс после клика (промежуточное состояние opacity/transform) — `page.screenshot(path="tc-form-011-open-mid.png")`.
  4. Закрыть («Отмена») — повторить наблюдение закрытия (скриншот промежуточного кадра закрытия).
  5. Зафиксировать в протоколе: тип анимации (transition/animation), свойства, длительность; приложить скриншоты.
- **Ожидаемый результат:** у оверлея/модалки заявлен непустой transition/animation (шаг 2); промежуточное состояние анимации наблюдаемо при открытии и закрытии (шаги 3–4, скриншоты); механизм открытия и закрытия единый (зафиксировать). Минимальный порог — заявленная длительность > 0; конкретный стиль на усмотрение реализации (Д-5), в протокол — факт.
- **Тестовые данные:** оверлей `#task-form-overlay`; действия «Создать задачу», «Отмена».
