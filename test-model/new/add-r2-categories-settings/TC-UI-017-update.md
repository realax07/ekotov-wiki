# REVALIDATE UPDATE — TC-UI-017 (CHK-146)

> **REVALIDATE UPDATE.** Не новый кейс: файл-обновление существующего кейса `TC-UI-017` (`tests/web/test_navigation_search_ui.py::test_search_archived_task_builder_advanced_card`; кейс — `test-model/approved/e2e-critical-path/ui-01.md` §TC-ui-017). Источник: impact §2.1 (revalidate, pending_update); пункт чеклиста CHK-146. Обновление теста — qa_automation через qa_case_reviewer (G8).

- **CHK:** CHK-146
- **Change:** add-r2-categories-settings
- **Затрагиваемый кейс:** TC-UI-017 «Поиск архивной задачи: конструктор → advanced → карточка»
- **Причина обновления (impact §2.1):** `page.get_by_label("Категория").fill("Дом")` на фильтре-конструкторе упадет при select-поле; задача с категорией «Дом» требует seed справочника. Шаги advanced/архива не затронуты.

## Таблица «было → станет»

| # | Шаг/ассерт кейса (было) | Станет (после обновления) |
|---|---|---|
| 0 (подготовка) | Создание `Архивная-поиск` с категорией `Дом` (через UI-форму) | **Среда:** категория «Дом» должна быть в seed справочника (CHK-139/TC-env-001 — иначе 422 на создание по FR-21). Способ ввода в подготовке — `select_option("Дом")` вместо fill (если подготовка через форму) |
| 1 | Конструктор: `page.get_by_label("Категория").fill("Дом")` | **ЗАМЕНИТЬ:** `page.get_by_label("Категория").select_option("Дом")` — выбор из select справочника («Дом» в seed) |
| 2 | `select_option("true")` «Архивность»; «Найти» | Без изменений |
| 3–6 | Результаты, бейдж «Архивная», advanced-фильтр `category = "Дом"`, правка фильтра, нормализация | Без изменений (advanced-текст фильтра по-прежнему содержит `category = "Дом"` — семантика фильтра не меняется от типа поля ввода) |
| 7 | Открытие карточки архивной задачи с признаками | Без изменений |

## Критерий приемки обновления

- В тесте нет ни одного `.fill()` на поле категории фильтра — только `select_option`.
- Зависимость от seed («Дом» в справочнике) зафиксирована в фикстуре/комментарии теста (CHK-139).
- Шаги advanced/архива (2–7 старой нумерации) не изменены — diff правки ограничен способом ввода категории (impact: «обновить только способ ввода категории»).

## Ожидаемый результат обновленного кейса (GIVEN/WHEN/THEN)

- **GIVEN** архивная задача с категорией «Дом» существует (категория в seed справочника)
- **WHEN** фильтр-конструктор: категория выбирается select_option («Дом»), архивность = true, «Найти»
- **THEN** результаты содержат только совпавшие; advanced-фильтр содержит `category = "Дом"`; карточка архивной задачи открывается с признаками — вся остальная цепочка без изменений
