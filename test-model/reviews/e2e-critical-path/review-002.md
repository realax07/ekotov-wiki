# Ревью 002: E2E-кейсы критического пути (e2e-critical-path) — повторное

- **Дата:** 2026-09-19
- **Ревьюер:** QA-ревьюер тест-кейсов (независимый сабагент, кейсы не писал)
- **Объект:** диф коммита 8b3c3e3 (`test-model/new/e2e-critical-path/ui-01.md`, +13/−12) против 5 замечаний review-001 (f55b84a). Полный файл ревьюен в review-001 — здесь только диф.
- **Вердикт: ОДОБРИТЬ.** Оба major устранены корректно, minors устранены, диф не выходит за отмеченные места, ворота флоу зеленые. Кейс перенесен в `test-model/approved/e2e-critical-path/`.

## 1. Проверка устранения замечаний review-001 (диф 8b3c3e3)

| № (001) | Кейс | Серьезность | Проверка исправления | Статус |
|---|---|---|---|---|
| 1 | TC-UI-018 | **major** | Добавлен шаг 0 (подготовка): `Fast-первая` из TC-UI-010 переводится в «Выполнено» через селект «Столбец» → done (путь тот же, что в TC-UI-012/013 — реализуемо); контроль перед шагом 1: fast-карточек в АКТИВНЫХ столбцах (`[data-status="todo"], [data-status="in_progress"]` × `article[data-fast="true"]`) → `to_have_count(0)`. Оговорка в шаге корректна: `Fast-первая` в «Выполнено» сохраняет `data-fast`, поэтому контроль ограничен активными столбцами (ACTIVE_STATUSES = todo/in_progress, backend/app/tasks.py) — иначе был бы ложный fail. Предусловие дополнено причиной (без шага 0 — 409). Ожидаемый результат дополнен шагом 0 | **устранено** |
| 2 | TC-UI-008 | **major** | Шаг 4: допустим ЛЮБОЙ механизм — (а) нативная валидация required-поля через `validationMessage` (непустая строка) ИЛИ (б) `expect(#task-form-error).to_be_visible()`; фактический механизм фиксируется в протоколе. Ожидаемый результат переформулирован зеркально. Ключевые проверки сохранены без изменений: POST `/api/tasks` не уходит (шаг 5 + ожидаемый), форма открыта, карточек нет — соответствуют рекомендации 001 | **устранено** |
| 3 | TC-UI-010, TC-UI-011 | minor | `.task-fast-badge` → `article[data-fast="true"]`: TC-UI-010 предусловие и TC-UI-011 шаг 7 заменены (в старой версии вхождений бейджа было два, третье место — новый контроль в шаге 0 TC-UI-018, тоже на data-fast). Обоснование контракта разметки (`renderCard` в board.js, соглашение п.2) добавлено. Стилевые классы `task-card-fast`/`has-fast` не тронуты — оговоренные исключения review-001 (п. «Селекторы»), не замечание №3 | **устранено** |
| 4 | TC-UI-013, TC-UI-015 | minor | «Подождать 1 с» удалены (в файле не осталось ни sleep, ни фиксированных пауз — проверено grep). Заменены на автожидание позитивного состояния через `expect` (TC-UI-013: `#board[data-loaded="true"]` + карточка видима; TC-UI-015: карточка видима) с последующей фиксацией `null_moves == []` / `null_comments == []` — ровно по рекомендации 001. Guard-критерий TC-UI-013 («запрос не уходит, доска не ломается», молчаливый guard, примечание по реализации) не тронут — сверено построчным дифом секции | **устранено** |
| 5 | TC-UI-013, TC-UI-015 | minor | Перехват 422 конкретизирован в обоих предусловиях: `responses_422 = []`, `page.on("response", lambda r: responses_422.append(r.url) if r.status == 422 and "/api/tasks/" in r.url else None)` — дословно по рекомендации; ожидаемые результаты синхронизированы (`responses_422 == []` вместо голословного «ни одного 422») | **устранено** |

## 2. Проверка дисциплины дифа

- 13 измененных строк — только в 5 отмеченных местах: TC-UI-008 (шаг 4 + ожидаемый), TC-UI-010 (предусловие), TC-UI-011 (шаг 7), TC-UI-013 (предусловие + шаг 2 + ожидаемый), TC-UI-015 (предусловие + шаг 2 + ожидаемый), TC-UI-018 (предусловие + шаг 0 + ожидаемый). Изменения ожидаемых результатов TC-UI-013/015 — часть правок №4/№5 (ссылка на новый `responses_422`), за объем не выходят.
- Остальные 15 кейсов не тронуты (сверено построчным diff --unified=0: все ханки в перечисленных секциях).
- Трассировка 18:18 сохранена (программная сверка после дифа: 18 TC ↔ 18 CHK-E, 1:1, без дублей).
- Ворота: `flow_check.py` — OK; `openspec validate --all --strict` — 8 passed, 0 failed (проверено лично до записи ревью).

## 3. Итог

- Blocker — 0, major — 0, minor — 0. Все 5 замечаний review-001 устранены; замечаний по дифу нет.
- Эскалация CHK-E-13 (молчаливый guard) из review-001 сохраняется как процессный вопрос чеклиста — решается ПМ/Заказчиком вне кейса; вердикту не мешает (кейс принял критерий «запрос не уходит» еще в 001).
- **Перенос выполнен:** кейс перемещен в `test-model/approved/e2e-critical-path/` (коммит «E2E-G1 review: verdict 002»).
- История: review-001 и review-002 сохранены в `test-model/reviews/e2e-critical-path/`; append-only соблюден.

## 4. Что проверено лично

- Построчный диф 8b3c3e3 против 5 замечаний (все 13 ханков прочитаны, сопоставлены с рекомендациями review-001).
- Отсутствие `.task-fast-badge` и фиксированных пауз в итоговом файле (grep).
- Контроль шага 0 TC-UI-018 на ложный fail: scope активных столбцов против ACTIVE_STATUSES и сохранения `data-fast` у задачи в «Выполнено» (board.js renderCard).
- Guard-секция TC-UI-013 построчным diff: критерий и примечание по реализации без изменений.
- Трассировка 18 TC ↔ 18 CHK-E программно; `flow_check.py` и `openspec validate --all --strict` — зеленые.
