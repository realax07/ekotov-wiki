# Ревью 001: тест-кейсы add-suggestions (дыра покрытия R1.3, CHK-S-6…9)

- **Дата:** 2026-09-21
- **Ревьюер:** QA-ревьюер тест-кейсов (независимый сабагент, кейсы не писал)
- **Объект:** `test-model/new/add-suggestions/sugg-01.md` — 4 TC (TC-sugg-006…009, коммит 95dadac)
- **Арбитры:** чеклист `test-model/checklists/add-suggestions.md` (целевые CHK-S-6…9 цикла 1.5); спека-дельта `openspec/changes/add-suggestions/specs/search/spec.md` (8 сценариев; целевые — Scenario 6, 7); `sdd.md` §3.2, §4; `design.md` add-suggestions §1–2
- **Вердикт: ОДОБРИТЬ** (blocker — 0, major — 0, minor — 5). Кейсы переносятся в `test-model/approved/add-suggestions/`; minor-замечания обязательны к учету автоматизатором (см. §4).

## 1. Сверка покрытия с чеклистом (проверено попарно, не на глаз)

- TC-sugg-006 → CHK-S-6 (осиротевшие теги + пустые категории исключены) — Scenario 6 спеки;
- TC-sugg-007 → CHK-S-7 (подсказки не создают новых значений) — Scenario 7 спеки;
- TC-sugg-008 → CHK-S-8 (граничное: категории пустые/NULL) — дополнение чеклиста;
- TC-sugg-009 → CHK-S-9 (состояние: удаление задачи) — дополнение чеклиста.
- Соответствие 1:1 без дублей; обратных ссылок на несуществующие CHK нет; лишних кейсов без источника нет. Уникальные префиксы `QAT-SUGG`/`QAT-SUGG8`/`QAT-SUGG9` не пересекаются с префиксами dev-тестов (`QAT-` в tests/api/test_suggestions.py) — изоляция обеспечена.

**Дыра покрыта целиком:** осиротевший тег через каскад `task_tags` (TC-006 шаги 1, 3 + ТС-009), пустые категории — и NULL, и `""` (TC-006 шаг 2: NULL; TC-008 шаги 1–2: оба варианта), запрет создания значений (TC-007: UI-свободный ввод + API-контроль), удаление задачи (TC-009). CHK-S-13 (метод эндпоинта) — вне цели цикла, покрыт dev-тестами согласно инвентаризации чеклиста.

## 2. Замечания

| № | Кейс | Серьезность | Дефект | Рекомендация |
|---|---|---|---|---|
| 1 | Общие предусловия | minor | Предусловие ссылается на `owner_cookies.txt`, но рецепт его получения не приведен (нет команды входа). Пароль seed-пользователя в самом файле кейсов не назван | Добавить в предусловия явный шаг: `curl -s -c owner_cookies.txt -X POST {BASE_URL}/api/auth/login -H "Content-Type: application/json" -d '{"login":"owner","password":"…"}'` со ссылкой на источник пароля (`tests/api/conftest.py`, seed) |
| 2 | TC-sugg-007, шаг 1 | minor | Доступное имя поля указано как «Теги»; фактический текст label в `frontend/templates/search.html` — «Теги (через запятую)». Автоматизация с точным совпадением имени не найдет элемент | Указать доступное имя дословно: «Теги (через запятую)» (или оговорить substring-сопоставление) |
| 3 | TC-sugg-007, шаг 4; TC-sugg-009, шаг 4 | minor | Ожидание строгой идентичности полного списка (S0 = финальный; «прочие значения не искажены») чувствительно к посторонним записям на общей БД стенда между снимками — ложное падение при чужой активности. Спека требует лишь отсутствия новых значений | Свести жесткое сравнение к префиксной подвыборке (`QAT-SUGG*`): в подвыборке ничего не изменилось + тестируемые значения отсутствуют/удалены; полное `s == sorted(s)` сохранить как инвариант |
| 4 | TC-sugg-006, 007, 008 | minor | Тестовые задачи (`QAT-SUGG-пустая-кат`, задачи QAT-SUGG8) остаются после прогона — постоянное загрязнение источника подсказок на общем стенде (значения живут, пока задача существует) | Добавить финальный шаг уборки: `DELETE /api/tasks/{id}` для всех созданных id (как teardown в tests/web) — или явно оговорить допустимость остатков при уникальном префиксе |
| 5 | TC-sugg-008 | minor | Буквальное состояние CHK-S-8 «ВСЕ категории существующих задач пусты» на общем стенде невоспроизводимо; кейс сводит проверку к префиксной подвыборке `QAT-SUGG8` — трактовка корректна, но ее основание в кейсе не названо | Добавить одно предложение-обоснование: подвыборка эквивалентно проверяет фильтр `category IS NOT NULL AND != ''` на общем стенде |

Blocker не обнаружены. Явных противоречий спеке нет: ожидания 200/`{"suggestions": [...]}` совпадают с design.md §2 (по факту `1f11011`); ожидание 204 удаления и 404 последующего GET — sdd §3.2; каскадное поведение `task_tags`/сохранение строк `tags` — sdd §4 и `backend/app/db.py` (ON DELETE CASCADE, foreign_keys=ON). Ожидание свободного ввода (TC-007) зафиксировано по замечанию 3 чеклиста — требование выполнено.

## 3. Что проверено лично

- Полный текст sugg-01.md против полного текста спеки-дельты, чеклиста и design.md (не выборочно); сверка со Scenario 6–7 спеки дословно.
- Трассировка CHK↔TC попарно; отсутствие дублей и осиротевших ссылок.
- Контроль фактического кода, на который опираются кейсы: `backend/app/db.py` (таблицы `tags`/`task_tags`, ON DELETE CASCADE), `backend/app/tasks.py` (POST 201, `category: str | None` — пустая строка проходит валидацию; DELETE 204 физический), `backend/app/suggestions.py` (SQL: JOIN через task_tags, фильтр `IS NOT NULL AND != ''`, пересчет на каждый GET без кеша — подтверждает ожидания TC-006/008/009), `frontend/templates/search.html` + `frontend/static/js/search.js` (`datalist#tag-hints` на обоих полях, `list=`, плейсхолдеры, `loadSuggestions()` — однократно на загрузку страницы: перезагрузка в TC-007 шаге 3 — корректный механизм обновления datalist).
- Сверка с существующими dev-тестами: `tests/api/test_suggestions.py` (TC-API-SUGG-001…005) и `tests/web/test_search_suggestions_ui.py` (TC-UI-SUGG-001/002) — дублирования с новыми кейсами нет, дыра CHK-S-6…9 dev-тестами действительно не покрыта.
- Воспроизводимость предусловий через API (curl-рецепты в шагах, атомарность шагов, фиксация id, контрольные «до/после» в TC-009); проверяемость ожиданий (коды HTTP, тела, вхождения/отсутствия значений, инвариант сортировки); конкретность данных; явность негативных ожиданий (включая строко-подобные null в TC-008).
- Ворота флоу перед ревью: `openspec validate --all --strict` — passed (9/9); `python scripts/flow_check.py .` — OK.

## 4. Итог

- Статистика замечаний: **blocker — 0, major — 0, minor — 5**.
- Вердикт «одобрить»: кейсы воспроизводимы, проверяемы, соответствуют спеке, дыра CHK-S-6…9 покрыта полностью.
- Замечания №2, №3, №4 — обязательные указания автоматизатору при развертке TC в тесты (точное доступное имя поля; префиксное сравнение вместо строгой идентичности полного списка; уборка данных); №1, №5 — правка текста кейсов в следующей итерации редактирования либо учет при автоматизации.
- Перенос в `test-model/approved/add-suggestions/` выполняется этим ревью (коммит «review: add-suggestions cases (verdict 001)», без push).
- История: review-001 сохранен; при доработке автором — review-002.
