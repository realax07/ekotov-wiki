# Ревью 001: тест-кейсы add-kanban-core

- **Дата:** 2026-09-18
- **Ревьюер:** QA-ревьюер тест-кейсов (независимый сабагент, кейсы не писал)
- **Объект:** `test-model/new/add-kanban-core/` — 80 TC в 7 файлах (коммит 284d284)
- **Арбитры:** чеклист `test-model/checklists/add-kanban-core.md` (81 CHK, покрытие 31/31 Requirement, 64/64 Scenario); спеки-дельты `openspec/changes/add-kanban-core/specs/{auth,board,fastline,tasks,archive,search,navigation}/spec.md`; контракт API `sdd.md` r5; design.md r?
- **Вердикт: ВЕРНУТЬ** (1 major). После устранения — повторное ревью (review-002).

## 1. Сверка покрытия с чеклистом (проверено скриптом, не на глаз)

- TC в файлах: **80** (auth 17, board 10, fastline 12, tasks 16, archive 9, search 11, navigation 5).
- CHK в чеклисте: **81**, уникальных 81.
- Каждому CHK-N соответствует ровно один TC по полю `**CHK:**`; обратных ссылок на несуществующие CHK нет; дублей покрытия нет (1:1).
- Непокрыт **CHK-35 — отложен: DS-1**, правомерно (решение ПМ зафиксировано; в fastline-01.md отложка помечена в шапке и в подвале файла).
- Нумерация TC внутри файлов сквозная, идентификаторы доменов (`TC-arch-*`, `TC-fast-*`, `TC-nav-*`) согласованы с именами файлов.

Непокрытых CHK (кроме отложенного CHK-35) нет; лишних кейсов без источника нет.

## 2. Замечания

| № | Кейс | Серьезность | Дефект | Рекомендация |
|---|---|---|---|---|
| 1 | TC-search-010 (CHK-75), шаг 2 | **major** | Ожидаемый результат шага 2 (`?tag=urgent&tag=home` → задачи с тегом urgent **ИЛИ** home) опирается на семантику повторяемого query-параметра `tag`, которая НЕ определена ни в search/spec.md, ни в sdd §3.5 («`tag` (повторяемый)» — без И/ИЛИ), ни в design §6 («условия объединяются по И»). Читение «ИЛИ» дизайн-согласовано с примером advanced `tag IN ("home","car")`, но контрактом не зафиксировано. При фактической семантике «И» кейс даст ложный дефект. Поведение аналогично DS-3/DS-4 — неспецифицированное, но DS в чеклисте для него нет | По образцу DS-3: шаг 2 перевести в режим фиксации факта (зафиксировать фактическую семантику И/ИЛИ и выдачу в протоколе; проход/падение не трактовать) с эскалацией ПМ как нового DS; либо удалить шаг 2 — CHK-75 полностью покрывается шагом 1 (`priority=high&archived=false`) |
| 2 | TC-board-009 (CHK-26) | minor | CHK-26 требует «перевод из любого столбца (проверить из всех трех)»; кейс проверяет только два исходных столбца: `Быстро-1` из «Ожидает», `Быстро-2` из «В работе», `Быстро-3` — снова из «Ожидает». Источник «Выполнено» не покрыт (переход done→done) | Либо дать явную трактовку (done→done покрывается TC-board-008 как повторный move в тот же столбец — указать это в кейсе ссылкой), либо заменить `Быстро-3` на задачу, уже находящуюся в «Выполнено» сегодня, с шагом повторного действия |
| 3 | TC-tasks-016 (CHK-56) | minor | Дублирование: снимок `before_arch.json` предписан и в предусловии («зафиксировать ответ поиска before_arch.json»), и в шаге 1. Кроме того, предусловие «выполнен TC-tasks-015 (рестарт)» при том, что кейс выполняет собственный рестарт в шаге 2 — порядок чтения неоднозначен | Убрать фиксацию снимка из предусловия (оставить в шаге 1); переформулировать предусловие: «выполнен TC-tasks-015 или независимо подготовлена архивная задача (процедура в предусловии); рестарт выполняется шагом 2 настоящего кейса» |
| 4 | TC-arch-007 (CHK-63), шаг 3 | minor | Формулировка «Сместить done-момент для различимости: move → done заново» смешивает два действия: сам повторный move к done ничего не «смещает» — смещение делается UPDATE'ом в шаге 4. Читающий может выполнить лишний UPDATE после шага 3 | Переформулировать: «шаг 3: move → done заново, зафиксировать DA2. Шаг 4: сместить `done_at` на вчерашний день (UPDATE), открыть доску — архивация от последнего перевода» |
| 5 | TC-fast-007 (CHK-34) | minor | Стилистика: «в системе нет ни одной задач с titles…» — опечатки/англицизм в ожидаемом результате | «в системе нет ни одной задачи с title `Фаст-вторая-API2`/`Фаст-вторая-API3`» |

Blocker не обнаружены; кейсы DS-тематик (TC-tasks-004, TC-tasks-010/012, TC-search-009, TC-nav-005) соответствуют одобренной трактовке ПМ — фиксация фактического поведения без 5xx, без требования уточненных ожиданий до решения DS. Отдельно отмечаю: ожидания 404/422/409/400 в негативных кейсах имеют источник в sdd (§3 «Не найдено — 404», «Ошибки валидации — 422», §3.2 «409 fast line occupied», §3.5 «400 filter syntax»), расхождений со спекой нет.

## 3. Что проверено лично

- Полный текст всех 7 файлов кейсов (не выборочно) против полного текста 7 спек-дельт, чеклиста и sdd r5 (§3–§5, §7).
- Покрытие CHK↔TC сверено программно (парсинг полей `**CHK:**` и заголовков `## TC-*`): 80 TC, 81 CHK, 1:1 без дублей, единственный непокрытый — отложенный CHK-35.
- Шесть осей по каждому кейсу: трассировка (CHK + Источник во всех 80); соответствие спеке (ожидаемые коды/тела сверены с sdd: 401-тела, 422/409/404/400, Set-Cookie флаги, форма ответов `columns`/`results`/`normalized_query`); воспроизводимость (предусловия с конкретными командами: seed, curl-сRecipe, способ истечения сессии через SQLite, эмуляция done_at); проверяемость (ожидания через код ответа/тело/поля Task; у НФТ-кейсов auth-017, tasks-015/016, arch-003 — измеримые пороги и «Метрика»); данные (конкретные значения во всех кейсах); негативные (явные ожидаемые ошибки, включая запрет 5xx).
- Ворота флоу: `openspec validate --all --strict` — passed; `flow_check.py` — OK (до внесения ревью).
- Обработка отложенного CHK-35 и DS-тематик — по решениям ПМ (не засчитано как дефект).

## 4. Итог

- Статистика замечаний: **blocker — 0, major — 1, minor — 4**.
- Вердикт «вернуть»: единственный major (TC-search-010, шаг 2) устраняется точечной правкой одного шага.
- Перенос в `test-model/approved/add-kanban-core/` НЕ выполняется — выполняется только при вердикте «одобрить».
- История: review-001 сохранен; следующая итерация — review-002 после доработки автора.
