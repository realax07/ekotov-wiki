# Ревью 003: тест-кейсы add-kanban-core (повторное)

- **Дата:** 2026-09-18
- **Ревьюер:** QA-ревьюер тест-кейсов (независимый сабагент, кейсы не писал)
- **Объект:** точечная доработка TC-arch-007 — коммиты c7566ab (правка кейса, `test-model/new/add-kanban-core/archive-01.md`, 5 строк) и 11e5eac (смоук-сценарий `backend/smoke_arch_007.py` как evidence к review-002). Объем ревью — только диф этих коммитов + отсутствие регрессии по замечаниям review-001.
- **Арбитры:** те же, что в review-001/002: чеклист `test-model/checklists/add-kanban-core.md` (81 CHK), `sdd.md` r5 (§3.2, §3.3, §4), `design.md` §5.
- **Вердикт: ОДОБРИТЬ.** Major review-002 (TC-arch-007) устранен, регрессии нет.
- **Ссылка на предыдущую итерацию:** `test-model/reviews/add-kanban-core/review-002.md` (вердикт «вернуть», 1 major — инвертированный порядок шагов 3/4 в TC-arch-007).

## 1. Проверка исправления major review-002 (TC-arch-007, CHK-63)

| Пункт | Статус | Проверка |
|---|---|---|
| Порядок шагов 3–5 (major review-002) | **УСТРАНЕН** | Новый порядок: шаг 3 — повторный move → done, `GET /api/tasks/{id}` → свежий `DA2` (сегодня, `DA2 ≠ DA1`, `archived_at=NULL`, в done-столбце); шаг 4 — UPDATE-смещение `done_at` на `<вчера>T18:00:00+03:00`, наблюдаемо через `GET /api/tasks/{id}` **до** какого-либо последующего move — смещение финально, перезаписать его нечем; шаг 5 — `GET /api/board` → ленивая архивация от смещенного `DA2`. Ровно рекомендация review-002 (вариант 1). Move в done выполняется один раз (шаг 3) и ДО смещения — противоречие sdd §3.2 («проставляется при move в done») устранено |

Согласованность ожидаемого результата с механикой:

- **sdd §3.3 / design.md §5:** формула в кейсе «`archived_at = now WHERE status='done' AND archived_at IS NULL AND done_at < начало текущего МСК-дня`» дословно совпадает с design §5 и sdd §3.3 (лениво при GET /api/board); `done_at` при архивации не трогается — кейс это и требует («`done_at` остался `DA2` (вчера)», сверка шагов 4/5). Часовая граница — МСК/UTC+3, как зафиксировано в sdd §3.3.
- **sdd §3.2:** шаг 3 проверяет проставление свежего `done_at` и пустой `archived_at` при move в done — соответствует; формулировка «в формате `+03:00`, как в TC-arch-002» устраняет форматный люк (урок review-6.1-001).
- **CHK-63:** «`done_at` корректен на каждом шаге, отсчет архивации от последнего перевода» — покрыт полностью: `DA1` → NULL → `DA2` свежий → `DA2` смещенный; различимость `DA2 ≠ DA1` и контроль отсчета именно от последнего перевода явные.
- **Доказательство исполнимостью:** смоук `backend/smoke_arch_007.py` воспроизводит шаги 3–5 кейса против реального продакшн-кода (TestClient, формат `+03:00` — как в кейсе). Запуск при ревью: **4/4 зеленых** (`done_at` снят при уходе из done; свежий `DA2` при повторном move; UPDATE-смещение наблюдаемо; ленивая архивация по смещенному `done_at`, задача исчезла со всех столбцов, `done_at` сохранен). Логика кейса подтверждена на коде, а не только на бумаге.

## 2. Проверка дифа

- `git show c7566ab --name-only`: изменен **ровно один** файл кейсов — `test-model/new/add-kanban-core/archive-01.md` (блок TC-arch-007, 5 строк). Остальные 6 файлов кейсов не тронуты.
- `git show 11e5eac --name-only`: только новый `backend/smoke_arch_007.py` (141 строка, addition-only, продакшн-код не меняет).
- Вне этих двух файлов коммитов с момента review-002 (50b694d) нет.

## 3. Отсутствие регрессии по 4 устраненным замечаниям review-001

| Пункт review-001 | Статус | Проверка текущего текста |
|---|---|---|
| 1 (major): TC-search-010 шаг 2 — семантика И/ИЛИ | **НЕ регрессировал** | Шаг 2 — по-прежнему DS-фиксация факта: «HTTP 200 (НЕ 5xx), тело — валидный ответ с полем `results`», семантика И/ИЛИ — фиксация в протоколе с эскалацией ПМ; формулировки «ИЛИ» в файле нет |
| 2 (minor): TC-board-009 — третий исходный столбец | **НЕ регрессировал** | `Быстро-3` в done с исходным столбцом «Выполнено», шаг 1 покрывает все три столбца, предусловие ссылается на CHK-26 «из всех трех» |
| 3 (minor): TC-tasks-016 — дубль снимка/предусловие | **НЕ регрессировал** | `before_arch.json` — только в шагах; предусловие самодостаточно (своя UPDATE-подготовка, рестарт — шаг 2, «зависимости от TC-tasks-015 нет») |
| 5 (minor): TC-fast-007 опечатка | **НЕ регрессировал** | «нет ни одной задачи с title …» — корректно |

Замечание 4 review-001 (атомарность шагов TC-arch-007) поглощено исправленным major — теперь каждый шаг атомарен.

## 4. Контрольные проверки

- Покрытие CHK↔TC сверено программно: 7 файлов, **80 TC, 80 уникальных CHK, 1:1 без дублей**; единственный непокрытый — отложенный CHK-35 (DS-1, правомерно, как в review-001/002); ложных ссылок на несуществующие CHK нет.
- Ворота флоу на текущем состоянии: `openspec validate --all --strict` — passed (1 item); `flow_check.py` — OK.

## 5. Итог

- Статистика замечаний итерации 003: **blocker — 0, major — 0, minor — 0**.
- Вердикт «одобрить»: все замечания review-001 (1 major, 4 minor) и review-002 (1 major) устранены, регрессий нет, логика TC-arch-007 согласована с sdd r5 §3.2/§3.3 и design §5 и подтверждена смоуком 4/4.
- Выполнен перенос всех 7 файлов кейсов из `test-model/new/add-kanban-core/` в `test-model/approved/add-kanban-core/` (коммит «QA-review: test cases 003»).
- История: review-001, review-002, review-003 сохранены (append-only).
