# REVALIDATE UPDATE — TC-UI-SUGG-002 (CHK-142)

> **REVALIDATE UPDATE.** Не новый кейс: файл-обновление существующего кейса `TC-UI-SUGG-002` (`tests/web/test_search_suggestions_ui.py::test_tag_hints_set_semantics_and_order`). Источник: impact §2.1 (revalidate, pending_update); пункт чеклиста CHK-142. Обновление теста — qa_automation через qa_case_reviewer (G8).

- **CHK:** CHK-142
- **Change:** add-r2-categories-settings
- **Затрагиваемый кейс:** TC-UI-SUGG-002 (утвержден, тест `test_tag_hints_set_semantics_and_order`)
- **Причина обновления (двойное сомнение — impact §2.1):** (1) среда: тест создает задачу с категорией `QAT-UI-Общее` вне справочника — после внедрения жесткой валидации FR-21 получит 422 на setup; (2) источник категории в datalist подсказок после внедрения справочника спекой прямо не определен (эскалация б).

## Таблица «было → станет»

| # | Шаг/ассерт кейса (было) | Станет (после обновления) |
|---|---|---|
| 1 | GIVEN: задача с категорией `QAT-UI-Общее` создается через API без подготовки справочника | **Среда:** перед созданием задачи — `POST /api/categories {"name":"QAT-UI-Общее"}` → 201 (категория в seed справочника / создается фикстурой; иначе 422 на setup по FR-21). Cleanup: категория удаляется после теста |
| 2 | Создание задачи: `category="QAT-UI-Общее"`, теги `QAT-UI-Тег-*` | Без изменений (категория теперь валидна по шагу 1) |
| 3 | Ожидание: категория `QAT-UI-Общее` присутствует в datalist `#tag-hints` | **Пометка «уточнить в sdd» (эскалация б):** ожидаемый состав подсказок написан по текущей реализации (значения задач: теги + категории задач, sdd §3.2 «GET /api/suggestions — без изменений»). Если реализация переведет категории подсказок на справочник — ожидаемый состав пересмотреть (и это внеплановый revalidate TC-API-SUGG — impact §2.1/эскалация 2) |
| 4 | Set-семантика и порядок подсказок | Без изменений |

## Критерий приемки обновления

- Setup теста не получает 422 после внедрения FR-21 (категория подготовлена шагом 1).
- В коде теста/кейса есть явная пометка «источник категорий подсказок — по текущей реализации (значения задач); уточнить в sdd» — чтобы смена источника при реализации была замечена и обработана как revalidate, а не как тихое падение.

## Ожидаемый результат обновленного кейса (GIVEN/WHEN/THEN)

- **GIVEN** категория `QAT-UI-Общее` есть в справочнике (seed/фикстура); задача с ней и тегами `QAT-UI-Тег-*` создана
- **WHEN** открыта страница поиска, datalist `#tag-hints` загружен
- **THEN** подсказки содержат значения задачи (включая `QAT-UI-Общее`) — по текущей реализации; set-семантика без дублей; порядок сортировки сохранен. При смене источника категорий на справочник — ожидание шага THEN пересматривается через sdd (эскалация б)
