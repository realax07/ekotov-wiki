# REVALIDATE UPDATE — TC-UI-018 (CHK-147)

> **REVALIDATE UPDATE.** Не новый кейс: файл-обновление существующего кейса `TC-UI-018` (`tests/web/test_navigation_search_ui.py::test_final_path_autoarchive_and_fast_release`; кейс — `test-model/approved/e2e-critical-path/ui-01.md` §TC-ui-018). Источник: impact §1.2 (revalidate, pending_update); пункт чеклиста CHK-147. Обновление теста — qa_automation через qa_case_reviewer (G8).

- **CHK:** CHK-147
- **Change:** add-r2-categories-settings
- **Затрагиваемый кейс:** TC-UI-018 «Финал пути: done сегодня видима → автоархивация → fast line освобождена»
- **Причина обновления (impact §1.2):** три создания fast через UI БЕЗ выбора приоритета («Fast-первая», «Fast-финал», «Fast-новая») зависят от новой механики формы: при реализованной блокировке поля форма сама подставляет priority=high при отметке fast → creations дают 201 + приоритет high; шаги 7–8 (201, «fast line занята» = 0) остаются валидными. Проверить после реализации формы; порядок валидации — CHK-138.

## Таблица «было → станет»

| # | Шаг/ассерт кейса (было) | Станет (после обновления) |
|---|---|---|
| 0 | Подготовка: `Fast-первая` (создана ранее в сюите, priority=high выбирался явно в TC-UI-010 helper) | **УТОЧНИТЬ:** с реализованной формой отметка fast подставляет high автоматически — явный `select_option("high")` в helper создания fast-задач можно опустить; факт подстановки зафиксировать ассертом значения поля перед сабмитом (или проверкой priority=high в API после создания — 201 + priority high) |
| 1 | Создание `Fast-финал` (fast ✔, приоритет `high` выбирается явно); move в done | **ЗАМЕНИТЬ:** создание fast БЕЗ выбора приоритета — форма подставляет high (CHK-129/CHK-134); контроль: после создания `GET /api/tasks/{id}` → `priority == "high"`. Move в done — без изменений |
| 2–6 | done-видимость, DB-крюк смещения done_at, reload, автоархивация, поиск архивной | Без изменений |
| 7 | Создание `Fast-новая` (fast ✔ без приоритета) → «Создать» | **УТОЧНИТЬ:** форма шлет priority=high (подставленный) → ожидание 201-пути валидно; дополнить ассертом `priority == "high"` у созданной задачи |
| 8 | Форма закрылась; `get_by_text("fast line занята")` → count 0; `Fast-новая` в «Ожидает», подсвечена | **УТОЧНИТЬ:** отсутствие «fast line занята» (422-путь не сработал — priority был high) + подсветка; проверить после реализации формы (порядок валидации — CHK-138) |

## Критерий приемки обновления

- Все три создания fast в цепочке идут БЕЗ явного выбора приоритета; хотя бы один ассерт `priority == "high"` присутствует (API после создания).
- Шаги 7–8 (201, «fast line занята» = 0) сохранены как валидные — помечены как зависящие от зафиксированного порядка валидации (CHK-138/TC-fast2-012).
- В docstring теста — пометка «исполнять после реализации формы (блокировка поля приоритета, CHK-129)».

## Ожидаемый результат обновленного кейса (GIVEN/WHEN/THEN)

- **GIVEN** fast line освобождена (шаг 0); форма создания открыта
- **WHEN** создаются fast-задачи без выбора приоритета (форма подставляет high и блокирует поле)
- **THEN** каждая fast создается 201 + priority=high; автоархивация и освобождение линии работают как прежде; после архивации новая fast создается успешно («fast line занята» нет) с приоритетом high
