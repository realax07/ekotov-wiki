# TC-form-006 — Крестик закрытия формы отображается в правом верхнем углу

- **CHK:** CHK-117
- **Change:** add-r2-categories-settings
- **Источник:** tasks: Крестик закрытия формы в правом верхнем углу (FR-28) / Scenario: Крестик закрывает форму
- **Тип:** позитивный | **Приоритет:** Should
- **Среда/предусловия:** поднятый стенд (`GET {BASE_URL}/api/health` → 200); Playwright (Python): `page`, `expect`; выполнен вход owner (логин `owner`, пароль `QaOwner_Pass_1!` — seed, как TC-UI-002 чеклиста e2e-critical-path); seed справочника выполнен (CHK-139 / TC-env-001).
- **Сценарий:**
  - **GIVEN** пользователь авторизован и открыл модальное окно формы задачи
  - **WHEN** пользователь просматривает окно
  - **THEN** крестик закрытия отображается в правом верхнем углу окна
- **Шаги:**
  1. Открыть `{BASE_URL}/board`; `page.get_by_role("button", name="Создать задачу").click()`.
  2. Найти крестик: `close = page.locator("#task-form-overlay").get_by_role("button").filter(has_text="×")` — при отсутствии текстового крестика осмотреть DOM-инспектором фактический элемент закрытия (кнопка/ссылка в заголовке модалки; локатор зафиксировать в протоколе как семантический якорь).
  3. Геометрия: `bb_close = close.bounding_box()`; `bb_modal = page.locator("#task-form-overlay").bounding_box()`.
  4. Скрипт в протоколе: правый край крестика близок к правому краю модалки (зазор < ~48px), верх крестика близок к верху модалки (отступ < ~48px).
- **Ожидаемый результат:** крестик виден; его позиция — в правом верхнем углу модального окна (оба порога соблюдены; точные значения зафиксировать в протоколе).
- **Тестовые данные:** модалка `#task-form-overlay` (семантический якорь, соглашение п.3 ui-01.md).
