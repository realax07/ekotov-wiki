# TC-form-007 — Нажатие крестика закрывает окно без сохранения

- **CHK:** CHK-118
- **Change:** add-r2-categories-settings
- **Источник:** tasks: Крестик закрытия формы (FR-28) / Scenario: Нажатие крестика закрывает окно без сохранения
- **Тип:** позитивный | **Приоритет:** Should
- **Среда/предусловия:** поднятый стенд (`GET {BASE_URL}/api/health` → 200); Playwright (Python): `page`, `expect`; выполнен вход owner (логин `owner`, пароль `QaOwner_Pass_1!` — seed, как TC-UI-002 чеклиста e2e-critical-path); seed справочника выполнен (CHK-139 / TC-env-001).
- **Сценарий:**
  - **GIVEN** открыто модальное окно формы задачи с заполненными полями
  - **WHEN** пользователь нажимает крестик в правом верхнем углу
  - **THEN** окно закрывается без сохранения введенных значений (задача не создана)
- **Шаги:**
  1. Сетевой журнал: `posts = []`, `page.on("request", lambda r: posts.append(r.url) if "/api/tasks" in r.url and r.method == "POST" else None)`.
  2. Открыть форму; заполнить название `QAT-крестик-не-создана`, описание `заполнено до закрытия`.
  3. Нажать крестик (локатор из TC-form-006).
  4. `expect(page.locator("#task-form-overlay")).to_be_hidden()`.
  5. `GET "/api/search?archived=false"` → посчитать задачи `QAT-крестик-не-создана`.
- **Ожидаемый результат:** шаги 3–4 — окно закрылось; `posts == []` (POST не уходил); шаг 5 — 0 совпадений — введенные значения НЕ сохранены, задача не создана.
- **Тестовые данные:** title=`QAT-крестик-не-создана` (НЕ должен создаться), description=`заполнено до закрытия`.
