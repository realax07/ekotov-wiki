# REVALIDATE UPDATE — TC-sugg-007 (CHK-143)

> **REVALIDATE UPDATE.** Не новый кейс: файл-обновление существующего кейса `TC-sugg-007` (`tests/web/test_search_suggestions_ui.py::test_free_input_does_not_change_suggestions_source`; кейс — `test-model/approved/add-suggestions/sugg-01.md` §TC-sugg-007). Источник: impact §2.1 (revalidate, pending_update); пункт чеклиста CHK-143. Обновление теста — qa_automation через qa_case_reviewer (G8).

- **CHK:** CHK-143
- **Change:** add-r2-categories-settings
- **Затрагиваемый кейс:** TC-sugg-007 «Подсказки не создают новых значений: свободный ввод вне datalist»
- **Причина обновления:** семантический конфликт со спекой справочника — существующий шаг 2 предполагает свободный ввод в поле «Категория» фильтра с ожиданием принятия ввода; новая спека (FR-19) прямо запрещает произвольный текст в поле категории (select). Кейс расщепляется ПО ДОМЕНАМ (эскалация в): теги — keep-семантика, категория — новая.

## Таблица «было → станет»

| # | Шаг/ассерт кейса (было) | Станет (после обновления) |
|---|---|---|
| 1 | Ввод свободного значения в поле «Теги» (`#search-tags`, datalist): `QAT-SUGG-Новый-Свободный` принят | **KEEP без изменений** — поле «Теги» сохраняет свободный ввод (легитимен в фильтре, FR-11); keep-семантика: ввод принят, новое значение не попадает в источник подсказок (снимок S0 идентичен) |
| 2 | Ввод свободного значения в поле «Категория» (`#search-category`, textbox с `list`): `QAT-SUGG-Новая-Кат-Свободная` принят | **ЗАМЕНИТЬ.** Поле категории теперь select: свободный ввод НЕВОЗМОЖЕН — ассерт меняется на негативный: (а) тег поля `SELECT` (fill() невозможен); (б) попытка `fill()` → исключение Playwright / ввод не принимается; (в) выбрать можно только значение из справочника (FR-19: произвольный текст не допускается) |
| 3 | Перезагрузка `/search`: опций свободных значений нет в datalist | Для тегов — без изменений. Для категории — опций `QAT-SUGG-Новая-Кат-Свободная` нет по построению (select не содержит значения вне справочника) |
| 4 | API-контроль: `GET /api/suggestions` идентичен снимку S0 | Без изменений (значения не «протекают» в источник подсказок) |

## Критерий приемки обновления

- Кейс (или пара кейсов) покрывает оба домена раздельно: «Теги» — свободный ввод принят и не создает значений; «Категория» фильтра — свободный ввод невозможен (select). Утверждение «ввод в поле Категория принят» из старой версии НЕ СОХРАНЯЕТСЯ.
- Ссылка на конфликт спек (эскалация в: сценарий 7 спеки add-suggestions vs FR-19) присутствует в комментарии теста; решение о сужении сценария 7 — за ПМ/Заказчиком, кейс написан по доменам (п.«Примечание» чеклиста).

## Ожидаемый результат обновленного кейса (GIVEN/WHEN/THEN)

- **GIVEN** пользователь авторизован, открыта страница поиска (конструктор); справочник непуст
- **WHEN** в поле «Теги» вводится значение вне datalist; в поле «Категория» предпринимается попытка свободного ввода
- **THEN** теги: ввод принят, подсказки/снимок S0 не изменились; категория: ввод невозможен — значение можно только выбрать из справочника; в источнике подсказок новых значений нет (п.4)
