# Бэклог улучшений продукта (ekotov-wiki)

> Заметки Заказчика и ПМ о развитии продукта. Формат: идея → проработка → статус. Это не задачи конвейера: пункт становится change-пакетом (requirements → sdd → задачи → QA) только после решения Заказчика о запуске. Порядок и приоритеты определяет Заказчик.

## Источник

- Заметки Заказчика (2026-09-19, после внедрения и первых сессий с продуктом).
- Пункт считается проработанным, когда сформулирован как требования (requirements.md) и прошел цикл конвейера.

## Блок P: Продуктовые улучшения (черновики)

### P1. Карточка/форма задачи — детальная проработка UX

**Заметки Заказчика:**
- Категории заранее задаются в настройках проекта (см. P5) — в форме выбор из списка, не свободный текст.
- Крестик закрытия модального окна — в верхний правый угол (сейчас расположение неудобно); проработать графику/визуал формы в целом.
- Теги подтягиваются из уже имеющихся (автодополнение, как в Jira): начинаешь вводить — показывает существующие теги проекта, можно выбрать или создать новый.
- Приоритеты — с цветом и иконкой (визуальное различение low/medium/high в форме и на карточке).
- Fast line: при создании fast-задачи приоритет автоматически «высокий», без возможности смены (быстрая задача = всегда высокая важность; снимает вопрос рассинхрона fast × priority).

**Доработка ПМ (интерпретация, требует подтверждения):**
- Автоподстановка priority=high для is_fast — это изменение спеки fastline (сейчас fast и priority независимы; CHECK в БД и sdd §3.2 допускают fast с любым приоритетом). Потребует дельты ADDED/MODIFIED в fastline + задачи в backend (валидация) и frontend (UI-lock поля).
- Автодополнение тегов — нужен API-эндпоинт «список существующих тегов проекта» (сейчас теги живут только внутри задач; GET /api/tags отсутствует).
- Иконки приоритетов — минимально: без внешних библиотек (inline SVG), чтобы не тянуть зависимости.

### P2. Раздел «Настройки» в сайдбаре

**Заметки Заказчика:** добавить раздел настроек, внизу слева, рядом с кнопкой выхода.

**Доработка ПМ:**
- Пока не определен состав настроек. Кандидаты из других пунктов: управление категориями проекта (P1/P5), возможно управление тегами. Требует решения Заказчика: какие настройки входят в первый релиз.
- Навигация: новый раздел = пункт сайдбара + страница — затрагивает домен navigation (спека: сайдбар, редиректы). Ленивая автоархивация не затрагивается.
- Открытый вопрос: настройки — общие (оба пользователя видят и меняют) или только для owner? Связано с моделью «общая доска» без разграничения прав — скорее общие, но подтвердить.

### P3. Профиль пользователя внизу сайдбара

**Заметки Заказчика:** отображать внизу слева имя пользователя и аватарку, настройки пользователя.

**Доработка ПМ:**
- Имя: сейчас у пользователей только login (owner/wife). Понадобится поле display_name (миграция схемы + API) или захардкоженные подписи («Владелец» / «Жена») — решить.
- Аватарка: внешний сервис (типа Gravatar) или загрузка файла? Загрузка = хранение файлов, новые NFR (размер, типы), новый эндпоинт. Минимум для первого шага: цветной кружок с первой буквой логина (без хранения файлов).
- «Настройки пользователя»: смена пароля — вероятный кандидат (сейчас пароли только через seed). Смена логина — вряд ли нужна (логины фиксированы по sdd).
- GET /api/auth/me — понадобится в любом случае (сейчас UI не знает, кто залогинен).

### P4. Подсказки в поиске (для полей теги/категории)

**Заметки Заказчика:** в поиске добавить подсказку для некоторых полей — теги, категории.

**Доработка ПМ:**
- Тот же механизм, что автодополнение в P1 (список существующих тегов/категорий) — реализовать один API и переиспользовать в двух местах (форма задачи + панель поиска).
- Для advanced-режима (SQL-подобный синтаксис) подсказки — отдельная история: dropdown с примерами синтаксиса или отложить.

### P5. Категории — справочник, задаваемый в настройках

**Заметки Заказчика:** категории — это выбор из списка; список задается в настройках проекта (см. P2).

**Доработка ПМ:**
- Новая сущность «категория проекта»: таблица categories, CRUD настроек, валидация задачи по справочнику (сейчас category — свободная строка в БД). Это изменение схемы + контрактов + спеки tasks (MODIFIED) — заметный change-пакет, самый крупный в списке.
- Миграция: существующие значения category в задачах (свободный текст) — маппить в справочник или оставить «как есть» с пометкой.
- Открытый вопрос: жесткая валидация (задача не создается с категорией вне справочника) или мягкая (предупреждение)?

### P6. Архитектурное разбиение крупных разделов на модули

**Заметки Заказчика:** разбить крупные разделы (строки из сайдбара) на модули в репозитории.

**Доработка ПМ:**
- Трактовка: сейчас frontend/templates/ и static/js/ — плоские файлы (board.html+board.js — самый крупный, ~600 строк JS). «Разбить на модули» = по разделу сайдбара (board / search / wiki) — своя папка: templates/board/, static/js/board/ (index, card, comment, modal...).
- Связано с G6/G9 из конвейерного бэклога (ветки, жизненный цикл артефактов): рефакторинг удобнее делать в ветке.
- Требование обратной совместимости: URL статики (/static/js/board.js) используются в шаблонах — при разбиении обновить пути; дымовой прогон после.
- Кандидат на первый шаг: board.js → модули (карточка, модалка формы, карточка детали/комментарии, фильтры). Без сборщика (ES-модули или конкатенация в том же порядке) — решить при проработке; сборщик (bundler) тянет Node-инфраструктуру, для двух пользователей избыточен.

### P7. Drag-and-drop плиток на доске

**Заметки Заказчика:** перетаскивание плиток по колонкам: при зажатии плитка прикрепляется к курсору и исчезает с доски; при отпускании — падает в зону столбца; при отсутствии зоны — возвращается на место.

**Доработка ПМ:**
- Эквивалентно move через селект (задача 4.4) — тот же API `POST /api/tasks/{id}/move`, новое только UI-представление. Дельта спеки не нужна (поведение то же, новое средство).
- Технология: HTML5 Drag and Drop API (нативно, без библиотек) — плитка `dragstart`, колонка `dragover` (подсветка зоны-приемника), `drop` (move + refresh). «Прикрепляется к курсору» — через `setDragImage` или CSS-клон; «исчезает с доски» — opacity/dispay при dragstart.
- Fallback: перенос недоступен на touch-устройствах без библиотеки — для двух пользователей (десктоп) приемлемо, зафиксировать как ограничение.
- Взаимодействие с fast line: drop в активную фазу при существующем fast — 409 (инвариант сохраняется), показать ошибку и вернуть плитку.
- Тесты: Playwright `drag_and_drop` + проверка 409-инварианта; E2E-сценарий в критический путь.
- Зависимость: после P6-рефакторинга (карточки в cards.js — добавлять DnD туда же).

### P8. Дизайн доски — эффекты и стили

**Заметки Заказчика:** улучшить дизайн доски, добавить больше эффектов, стилей.

**Доработка ПМ:**
- Формулировка открытая — требует конкретики: анимации переходов (hover карточек, плавный move между колонками), тени/глубина, скругления, цветовые акценты приоритетов, анимация появления карточек, красивый пустой state.
- Технологии: чистый CSS (transitions, keyframes) — без библиотек; переменные CSS для темы (потенциально темная тема в будущем).
- Предлагаемый процесс: Заказчик смотрит 2–3 варианта стиля (мокапы или живые пресеты), выбирает направление → затем внедрение.
- Не забыть: доступность (контраст), производительность (анимации без layout-thrashing).

### P9. Дизайн карточки редактирования (модальной формы)

**Заметки Заказчика:** поработать над карточкой редактирования, улучшить дизайн.

**Доработка ПМ:**
- Пересекается с P1 (визуал формы: крестик в угол, цвета/иконки приоритетов) — P9 = общий редизайн модалки, P1 = функциональные улучшения той же модалки. Объединить в один цикл работ над формой.
- Состав: сетка полей, типографика, отступы, состояние ошибок (красные подсветки), состояния кнопок, анимация открытия/закрытия, адаптив под узкие экраны.
- Зависимость: после P6 (модули) и логично вместе с P8 (единый стиль).

### Обновление приоритизации

Визуальный блок (P7/P8/P9) естественно идет после P6-рефакторинга и вместе с P1: drag-and-drop в cards.js, редизайн формы в task-form.js, общий стиль P8 связывает. Предлагаемая цепочка: **P6 → P7+P8+P9+P1 (визуально-функциональный релиз) → P5+P2 (справочники/настройки) → P3 → P4**. Альтернатива: визуал (P7/P8/P9) отдельным быстрым релизом сразу после P6 — даст быстрый видимый эффект Заказчику.

Естественная зависимость: **P5 (справочник категорий) → P1 (форма)** и **P5 → P2 (настройки)** — справочник нужен обоим. **P3 (профиль)** и **P4 (подсказки)** — независимы. **P6 (рефакторинг)** — выгодно сделать до P1/P2/P4, чтобы новые UI-фичи сразу писать в модульной структуре, а не переписывать.

Возможный порядок: P6 (рефакторинг) → P5+P2 (категории + настройки) → P1 (форма) → P3 → P4.

## План релизов (утвержден Заказчиком 2026-09-20)

### Релиз 1 — «Обкатка контура + фундамент»

Тема: первый цикл по новому процессу (ветки/PR/RC) на мелких-средних задачах + подготовка почвы.

| # | Задача | Источник | Объем | Статус |
|---|---|---|---|---|
| 1.1 | BUG-001: клик по карточке из поиска (click-обработчик renderCard, монтирование модалки на /search) | BUG-001 | S | ✅ закрыта (f6baa75, merge f3248d9, прод обновлен) |
| 1.2 | P6-частично: board.js → ES-модули (api, dom, state, cards, task-form, task-detail, board-init); search.js НЕ трогается (унификация хелперов — отдельная задача позже) | P6 | M | ✅ закрыта (63a205b, fc4d9ff; 18/18 web) |
| 1.3 | P4: GET /api/suggestions (set tags ∪ categories по уточнению Заказчика) + datalist на оба поля поиска | P4 | S-M | ✅ закрыта (1f11011, PR#1; ретро-Флоу 1 закрыт: requirements-p4, дельта, impact, чеклист 13 CHK, 4 кейса дыры, тесты+метки G5, архивация 23ae489) |
| 1.4 | H4: ruff + eslint в flow.yml | конвейер | S | ✅ закрыта (ae5b13a; базлайн-фиксы, RUFF/ESLINT CLEAN, регресс 21+85/5) |
| 1.5 | QA-цикл пакета: impact-анализ (H5 первый запуск) → чеклист → кейсы (H1: 1 кейс = 1 файл) → ревью → тесты с метками regression: keep/archive (G5 первые метки) | конвейер | M | ✅ выполнен в ретро-цикле R1.3 (impact: keep 12/0/0; чеклист 13 CHK-S; кейсы дыры approve; тесты 4 шт + метки) |
| 1.6 | Чистка (G9 первый запуск): 13 smoke-скриптов backend/ (вкл. 3 DOM-варианта, пойманы ПМ), дубль new, деплой на main | G9 | S | ✅ закрыта (578f6ec; прод пережил ребут, все проверки зеленые) |

Выход Релиза 1: модульный frontend, подсказки тегов, BUG-001 закрыт, первый опыт предрелизного прогона на RC-ветке + первая чистка. Обкатаны: ветки/PR, H5, метки G5.
Границы: поведение продукта меняется только в 1.3 (новый эндпоинт + datalist) — остальное структура/инфраструктура. search.js хелперы НЕ унифицируются (осознанный YAGNI до P4-доработок).

### Релиз 2 — «Категории + настройки + форма» (черновик, старт по согласованию)

| # | Задача | Источник | Объем |
|---|---|---|---|
| 2.1 | P5: справочник категорий (таблица categories, CRUD, миграция значений) | P5 | L |
| 2.2 | P2: раздел «Настройки» (сайдбар, страница, управление категориями) | P2 | M |
| 2.3 | P1-1: форма — выбор категории из справочника + валидация | P1 | M |
| 2.4 | P1-2: теги-автодополнение в форме, priority=high lock для fast (дельта fastline) | P1 | M |
| 2.5 | P1-3: визуал формы (крестик в угол, цвета/иконки приоритетов, inline SVG) | P1 | S-M |
| 2.6 | QA-цикл (impact-аналитик ключевой: 2.4 меняет поведение fastline) | | L |

Открытые вопросы Релиза 2 (решить до старта): жесткая/мягкая валидация категории; настройки общие или только owner; миграция существующих category-текстов.

**Отложено вне релизов:** P3 (профиль: display_name, GET /api/auth/me, смена пароля) — независим, старт когда угодно; advanced-подсказки синтаксиса — сомнительная ценность.

## Открытые дефекты продукта (найдены после релизов)

- **DEF-001 (2026-09-21, после Релиза 1):** подсказки в поиске не подтягивают значения из базы — селект/поля должны показывать уникальные значения соответствующих столбцов (можно хранить предвычисленно). Затронуто: datalist R1.3 (P4) и/или select-поля категории. Требует диагностики на проде с реальной сессией: эндпоинт отвечает (401 без сессии подтвержден), но в UI значения не появляются — вероятные причины: (а) datalist не заполняется из-за ошибки JS при загрузке /search, (б) select категории вообще не привязан к данным (статические option'ы), (в) несоответствие формата ответа. → Диагностика на проде с сессией → фикс через Флоу 2 (если баг) или уточнение требований (если ожидался другой механизм — тогда Флоу 1). Закрепить также пожелание: «селект-поля должны тянуть уникальные значения соответствующих столбцов БД (можно предвычислять и хранить)» — это более общий механизм, чем datalist: применим и к категории, и к другим select-полям будущего (P5-справочник).
