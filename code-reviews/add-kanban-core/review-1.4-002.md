# Ревью задачи 1.4 — итерация 002 (повторное)

- **Change:** add-kanban-core
- **Задача:** 1.4 (systemd-юнит + конфиг nginx)
- **Первичное ревью:** [review-1.4-001.md](review-1.4-001.md) (верdict **return**: 1 major + 4 minor)
- **Диф исправления:** коммит `ff95f22` (task 1.4: fixes from review-1.4-001) — только `deploy/README.md`, +17/−8
- **Объем проверки:** только устранение замечаний 001; конфиги `ekotov-wiki.service` / `nginx-ekotov-wiki.conf` вне дифа (замечания 4–5 были опциональными, приняты как «осознанно не чинились»)
- **Метод:** три круга скилла code-review в объеме дифа + буквальное воспроизведение инструкции шага 5 ревьюером

## Вердикт: **approve**

Blocker/major нет, все замечания 001 устранены или корректно закрыты.

## Проверка замечаний 001

| # 001 | Серьезность | Статус | Проверка ревьюера |
|---|---|---|---|
| 1 | **major** (шаг 5: миграция невоспроизводима) | **устранено** | В README шаг 5 теперь начинается с `cd /opt/ekotov-wiki/backend`, флаг `--workdir` удален, путь к python относительный (`.venv/bin/python`). Воспроизведено буквально: `cd backend` + `env DB_PATH=<тестовая БД> SECRET_KEY=x <venv>/bin/python -m app.db` → rc=0, «Схема применена», таблицы созданы (`users, sessions, tasks, tags, task_tags, comments`); повторный запуск → rc=0 (идемпотентность подтверждена). Команда работает из чистой директории. ✅ |
| 2 | minor (нерелевантный пакет в fallback шага 8) | **устранено** | `libnginx-mod-http-headers-more-filter` убран; вместо него корректное указание, что `options-ssl-nginx.conf`/`ssl-dhparams.pem` обычно ставит `python3-certbot-nginx` (он и правда в списке шага 1), и два рабочих варианта: убрать `include`/`ssl_dhparam` либо сгенерировать dhparam через `openssl dhparam`. ✅ |
| 3 | minor (seed-инструкция неоднозначна) | **устранено** | Шаг 5 теперь явно: миграция и seed от `wiki` через `sudo -u wiki` (БД и WAL-файлы сразу с нужным владельцем, `chown` из старого текста убран); `SECRET_KEY=x` помечен как заглушка «только для разового запуска, прод — в .env»; seed описан точно: «РОВНО 2 учетки — owner (владелец) и wife (жена), интерактивный, пароль с терминала без эха, интерактивная сессия». Соответствует фактическому поведению `app/seed_users.py` (логины фиксированы owner/wife, `getpass`). ✅ |
| 4–5 | minor, опциональные (nginx: server_tokens, gzip/BREACH) | не в объеме | Конфиги не тронуты: `git log -- deploy/*.service deploy/*.conf` — последний коммит `24b02d3` (исходный), `ff95f22` меняет только README. Согласовано с итогом 001 («№4–5 — опциональная полировка»). ✅ |

## Факт-чек ревьюера (воспроизведено лично)

- Миграция: чистая тестовая БД + команда по инструкции (эквивалентный venv — в dev-окружении репозитория нет `backend/.venv`, собран по `requirements.txt`; форма команды идентична) → rc=0, схема применена; повторный прогон → rc=0.
- Seed: `python -m app.seed_users` — интерактивный (getpass, без эха), при пустом вводе/небезопасном stdin корректно отказывает; при вводе паролей созданы ровно 2 записи: `owner`, `wife` с bcrypt-хешами. Описание в README совпадает с поведением.
- Диф `ff95f22`: только `deploy/README.md`; systemd/nginx конфиги не изменялись.
- Обработка ошибок/секреты в новой части README: новых секретов нет, `SECRET_KEY=x` явно помечен заглушкой.

## Что НЕ проверено (честно)

- Реальный VPS-прогон шагов 1–9 целиком (вне скоупа итерации 002; проверялось в 001 эмуляцией).
- Поведение `sudo -u wiki` с сохранением cwd на конкретной конфигурации sudo VPS (стандартное поведение sudo cwd сохраняет; относительный `.venv/bin/python` при cwd=`/opt/ekotov-wiki/backend` валиден — проверено локально).

## Итог

Замечания 001 №1–3 устранены и подтверждены воспроизведением; конфиги не тронуты, как и согласовано. Задача 1.4 может вливаться.
