# Ревью задачи 4.4 + 6.1 — итерация 002 (повторное, по fix-коммиту 9fdbb86)

- **Диф:** коммит `9fdbb86` («task 6.1: fixes from review-6.1-001»), проверен
  относительно состояния `ffef741` (первичное ревью 001).
- **Арбитры:** sdd.md r5 (§3.2, §3.3, §4), design.md §5, дельты specs archive/board.
- **Вердикт: APPROVE** — все 4 замечания 001 (1 blocker + 3 minor) устранены,
  новых blocker/major не найдено.
- Ревьюер: код-ревьюер (изолированная сессия). Все проверки выполнены лично,
  venv `/home/openclaw/venvs/wiki`, TestClient на реальном app.
- Независимый проверочный скрипт ревьюера: `backend/reviewer_002_checks.py`
  (не коммитится, рабочий артефакт сессии; в дерево задач не входит).

## Судьба замечаний review-6.1-001

| # 001 | Серьезность | Статус | Подтверждение (лично) |
|---|---|---|---|
| 1 | blocker (UTC vs +03:00 в done_at/границе) | **устранен** | `tasks.py` move пишет `done_at = msk_now_iso()`; `board.msk_now_iso()` — `datetime.now(utc).astimezone(MSK).isoformat()`; `_msk_day_start_iso()` — тот же тик `astimezone(MSK)` + `replace(0,0,0,0)`. Единый источник, оба формата `...+03:00` (grep: других записей done_at нет). Блокер-кейс из 001 воспроизведен: см. A ниже. |
| 2 | minor (смоук маскировал blocker строкой +00:00) | **устранен** | `smoke_6_1.py`: `set_done_at()` теперь `assert iso.endswith("+03:00")` — подмена чужого формата невозможна; негативные кейсы — сдвигом datetime от `msk_now_iso`; добавлены кейсы 23:59:59.999 вчера / 00:00:00.000 сегодня / move при 01:00 МСК. Прогон: 17/17, EXIT=0. |
| 3 | minor (неверный комментарий db.py) | **устранен** | Комментарий теперь описывает PRAGMA-проверку в init_db как механизм идемпотентности и прямо говорит, что сам ALTER идемпотентным не является. Соответствует коду. |
| 4 | minor (мертвый CSS `.board-done-note`) | **устранен** | grep `board-done-note` по frontend/static/css/board.css и backend/smoke_6_1.py — пусто. В продакшн-статике класса нет (остался только в фикстуре DOM-смоука 5.2 — тестовая строка, не продакшн; смоук 5.2 при этом зеленый). |

## Проверенные Scenario (лично, исполнением)

**A. Блокер-кейс 001 честным продакшн-путем (свои проверки, не смоук дева):**
- Подмена часов в ОБОИХ модулях (app.board + app.tasks) на «сегодня 01:00 МСК»,
  затем **реальный** POST move в done через API → done_at в БД
  `2026-09-18T01:00:00+03:00`, archived_at NULL, задача в done-столбце —
  **НЕ архивируется сегодня**. ✅ (001: архивировалась)
- Контроль чувствительности кейса: done_at в старом формате
  `2026-09-17T22:00:00+00:00` (ровно кейс из 001) при том же GET /api/board →
  archived_at проставлен, с доски исчезла — кейс ловит именно форматиный баг. ✅

**B. 24-часовой табличный прогон (продакшн-функции):**
- Для каждого из 24 часов МСК-дня: `msk_now_iso()` при часе H против
  `_msk_day_start_iso()` при том же подмененном времени, то же
  лексикографическое сравнение, что в SQL — **0 ложных архиваций**. ✅
- Все 24 done_at в формате `+03:00`; граница всегда `T00:00:00+03:00`. ✅
- Контроль: те же 24 часа со старым форматом `+00:00` → ложные архивации в
  часах 0–2 (баг-класс 001 воспроизводится на старом коде). ✅
- `_msk_day_start_iso()` при server-TZ `UTC+14` (Pacific/Kiritimati) и UTC+0 —
  ровно `00:00+03:00` текущего МСК-дня; тик у done_at/границы — один код
  (`astimezone(MSK)`). ✅

**C. Регрессия таблицы переходов 4.4 (реальные move через API, 10 проверок):**
- todo→wip→todo→wip→done→todo→done→wip→done: все 200; при не-done — оба поля
  NULL и задача в целевом столбце; при done — done_at свежий `+03:00`,
  archived_at NULL, задача в done-столбце; повторный done получает новый
  done_at. ✅
- move bogus → 422; move несуществующий id → 404. ✅

**Смоук дева (воспроизведен):** `smoke_6_1.py` 17/17, EXIT=0 — включая
5в (move при 01:00 МСК), границы 23:59:59.999/00:00:00.000, миграцию
(свежая БД / идемпотентность / ALTER pre-r5), схему GET task (12 полей).
Смоук 5.2 — зеленый (регрессия fast-колонок после удаления CSS-класса). ✅

**Ворота:** `openspec validate --all --strict` — passed (INFO-замечания об
архивации — известные, вне дифа); `flow_check.py` — OK. ✅

**Сверка со спекой (первый круг по диффиксу):** sdd §3.3 «выполненная сегодня
задача видна в столбце Выполнено» — теперь верно в т.ч. в 00:00–03:59 МСК;
дельта archive «до наступления следующего календарного дня» — соблюдена
(включая границу 00:00:00.000 — не архивируется); §3.2 move — семантика
done_at/archived_at не изменена, только формат; вне-спечного поведения в
диффиксе нет (комментарии/докстринги + сам фикс формата).

## Что НЕ проверено

- Реальный браузерный рендер (JS-исполнение) — как и в 001, вне диффикса;
  DOM-смоук 5.2 дева заявлен зеленым, мною не перегонялся.
- Продакшн-развертывание, NFR-замеры — вне дифа.
- Race-поведение под параллельными GET — проверялось в 001 (8 параллельных,
  чисто); диффикс меняет только формат строки, SQL-форму WHERE не трогает.

## Вывод

Исправление 9fdbb86 закрывает blocker 001 по существу: done_at и граница дня
теперь генерируются одной функцией в одном формате `+03:00`, лексикографическое
сравнение хронологически корректно во всех 24 часах МСК-дня; блокер-кейс из 001
(01:00 МСК) на реальном API больше не архивирует задачу, контроль на старом
формате подтверждает чувствительность проверки. Все три minor устранены.
Замечаний, препятствующих вливанию, нет. **Approve.**
