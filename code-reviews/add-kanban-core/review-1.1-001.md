# Code Review: задача 1.1 «Инициализировать проект» — review-1.1-001

- **Репозиторий:** ekotov-wiki, ветка main
- **Диф:** коммит `a49944a` «task 1.1: project skeleton» (13 файлов, +70/−1)
- **Арбитры:** requirements.md r3; openspec/changes/add-kanban-core/ (design.md, sdd.md, specs/); tasks.md
- **Процедура:** скилл code-review, три круга (спека → практики → integration)
- **Дата:** 2026-09-17

## Вердикт: **approve**

Задача 1.1 выполнена полностью и соответствует своему объему в tasks.md и sdd.md. Все
заявления dev-отчета проверены фактически (запуск процесса, health-запрос, негативные
сценарии env, check-ignore) — подтверждены. Blocker и major отсутствуют. Два вопроса,
поднятых dev к СА, обоснованы и эскалируются оркестратору отдельно (дефекты спеки, не
дефекты задачи — см. «Эскалация»).

## Круг 1 — соответствие спеке

| Пункт tasks.md 1.1 | Факт | Статус |
|---|---|---|
| Структура backend/, frontend/, deploy/ | `backend/app/{__init__,config,main}.py`, `frontend/{templates,static}/`, `deploy/*placeholder` | OK |
| Выбранный фреймворк | FastAPI + uvicorn — совпадает с sdd.md §1 | OK |
| Конфиг с путем к БД вне репозитория | `config.py`: `DB_PATH`/`SECRET_KEY` только из env, без дефолтов, fail-fast | OK |
| `.gitignore` дополнен `*.db`, `*.sqlite*` (NFR-5) | `*.db`, `*.sqlite`, `*.sqlite3` присутствуют; `git check-ignore` подтверждает (см. круг 2) | OK |
| Проверка: каркас стартует и отвечает health | Воспроизведено ревьюером: `GET /api/health` → 200 `{"status":"ok"}` | OK |

- Модальность: каркас затрагивает NFR-5 (Should) и ОГР-2 (API-слой отделен — `/api/*` префикс есть). Выполнено.
- Health-эндпоинт: требования «отвечает health-запросом» из tasks 1.1 достаточно для трассировки; поведение соответствует отчету dev.

## Круг 2 — practices / security (проверено исполнением, не по отчету)

| # | Заявление dev | Проверка ревьюера | Результат |
|---|---|---|---|
| 1 | health GET /api/health → 200 {status:ok} | Запущен uvicorn на 8137 с test-env, `curl` → HTTP 200 `{"status":"ok"}` | ✅ подтверждено |
| 2 | Без DB_PATH/SECRET_KEY процесс падает на старте | `env -i` (обе переменные отсутствуют) → `RuntimeError: Обязательная переменная окружения DB_PATH не задана…`; затем с DB_PATH без SECRET_KEY → та же ошибка по SECRET_KEY | ✅ подтверждено, сообщение понятное, без утечек |
| 3 | `git check-ignore` подтверждает *.db | `git check-ignore -v test.db foo.sqlite3 .env wiki.db x.sqlite` — все проигнорированы (.gitignore:5–8); в `git ls-files` нет ни .env, ни .db | ✅ подтверждено (NFR-5) |
| 4 | venv вне репо | `/home/openclaw/venvs/wiki`, в репозитории отсутствует | ✅ подтверждено |
| 5 | `.env` добавлен в .gitignore | .gitignore:8 `.env`; в индексе отсутствует | ✅ подтверждено |
| 6 | deploy/ — только placeholders | Два файла-плейсхолдера с комментариями «задача 1.4», секретов/реальных путей нет | ✅ подтверждено |

- Дефолтных секретов нет: `SECRET_KEY=` в .env.example пуст; скан репо на секреты — чисто. Тестовый SECRET_KEY для запуска генерировался одноразовый, в репо не попадал.
- OpenAPI/docs отключены (`docs_url=None, redoc_url=None, openapi_url=None`) — оправданное сужение поверхности для интернет-доступа (NFR-7), поведение не добавляет.
- Зависимости: точные пины `fastapi==0.141.1`, `uvicorn[standard]==0.53.0`, `jinja2==3.1.6`; установленные версии в venv совпадают пин-в-пин. Лишних зависимостей нет (jinja2 обоснован выбором стека sdd §1, шаблоны — задачи 2.3+).
- Кода вне трассировки нет: только каркас + health из текста задачи. `tasks.md` отмечен `[x]` в том же коммите — корректно.

## Круг 3 — integration-точки

- Смежных сценариев у каркаса нет (задача первая реализуемая). `GET /` и `/docs` → 404 — ожидаемо (страницы — задачи 2.3/3.x).
- Ворота флоу: `openspec validate --all --strict` — 1 passed, 0 failed; `flow_check.py` — OK. Рабочее дерево чистое после ревью.

## Таблица замечаний

| # | Серьезность | Файл | Строка | Замечание | Рекомендация |
|---|---|---|---|---|---|
| 1 | minor | sdd.md | §3 (весь раздел) | Эндпоинт `GET /api/health` не описан в API-контрактах sdd.md — реализован по tasks 1.1, но в контракте отсутствует | Оркестратору: поручить СА добавить health в sdd.md §3 (метод/путь/ответ 200/exempt от auth) при следующей правке пакета |
| 2 | minor | sdd.md §3 + specs/auth/spec.md:50 | 40 | Внутреннее противоречие спеки: «любой API-запрос без сессии → 401» vs health из tasks 1.1, обязанный отвечать 200 без сессии | Оркестратору: до задачи 2.2 (middleware) зафиксировать в sdd §3/дельте auth exempt-список (минимум `/api/health`); это дефект спеки — эскалация, не возврат задачи 1.1 |
| 3 | minor | .gitignore | 5–8 | Строки `*.db`, `*.sqlite`, `*.sqlite3` внесены коммитом 6f214e0 (до change-пакета), а не задачей 1.1; коммит 1.1 добавил только `.env` | Информационное: итоговое состояние NFR-5 корректно, проверка tasks 1.1 выполняется; в трассировку изменений правки не требуется |

## Проверенные сценарии

- Позитивный: каркас стартует, `GET /api/health` → 200 `{"status":"ok"}` (воспроизведено).
- Негативный: отсутствие DB_PATH → падение на старте с понятным сообщением (воспроизведено).
- Негативный: отсутствие SECRET_KEY (при заданном DB_PATH) → падение на старте (воспроизведено).
- NFR-5: `*.db`/`*.sqlite*`/`.env` игнорируются git, в индексе отсутствуют (воспроизведено).

## Что НЕ проверено (честно)

- Развертывание (systemd/nginx) — вне объема 1.1, задача 1.4.
- Поведение приложения с БД — БД в каркасе еще не создается (задача 1.2); корректность DB_PATH по факту подключения не проверяема.
- Перезапуск/персистентность (NFR-3) — не применимо к каркасу.
- На 2.2 останется проверить, что middleware не зацепит exempt `/api/health` (после фикса замечания 2).
