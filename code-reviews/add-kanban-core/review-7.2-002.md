# Ревью 7.2 (002) — повторное ревью исправления ff78461

- **Задача:** 7.2 (tasks.md, FR-12) — исправляющий коммит `ff78461`
- **Первичное ревью:** `review-7.2-001.md` (verdict **return**: 1 major + 2 minor)
- **Ревьюер:** код-ревьюер (изолированная сессия, независимая валидация)
- **Диф исправления:** `backend/app/search.py` (+~50/−22), `backend/smoke_7_2.py` (+32) — подтверждено `git show ff78461 --name-only`: других файлов диф не задевает; рабочее дерево чистое
- **Арбитры:** sdd.md §3.5, specs/search/spec.md, tasks.md 7.2

## Вердикт: **APPROVE**

Все три замечания 001 закрыты, каждое проверено независимо (не на слово dev).
Регрессия зеленая. Новых замечаний нет.

## Проверка закрытия замечаний 001 (лично, не по отчету dev)

| # 001 | Серьезность | Статус | Как проверено |
|---|---|---|---|
| 1 | major (битый JSON → 500) | **Закрыт** | Независимый прогон на TestClient: `{not json` → **400** `{"error":"filter syntax: invalid JSON body"}` — точное тело. Дополнительно: `b""`, `{`, `{"query":}` → 400; валидный JSON с query-не-строкой (`[]`, `"str"`, `null`) → 422 sdd-формата; 401 без сессии приоритетнее и битое тело не вызывает 500. Обработка — try/except вокруг `request.json()` до всякого `get_connection`. |
| 2 | minor (дубликаты молча перезаписывались) | **Закрыт** | 16 комбинаций дубликатов → **400** «duplicate field X»: priority (=/!=, все 4 пересечения `!= X AND priority = Y` включительно), category (3), tag IN, archived, due (5: `<=`, `>=`, `=`, `=`+`>=`, `>=`+`=`), due_before, due_after. Легитимный диапазон `due >= 2026-09-01 AND due <= 2026-09-30` → **200**, результаты верны (фильтрует по реальным данным), normalized_query каноничен. `due … AND due_before/due_after` (разные слоты) → 200 — согласуется с решением, задокументированным в docstring. Одиночные условия всех полей → 200. |
| 3 | minor (field_pos — линейный поиск по токенам) | **Закрыт** | Код: `_parse_condition` возвращает `field_tok.pos` 5-м элементом кортежа, `_apply_predicate` принимает `pos: int` параметром — вложенная функция `field_pos()` и параметр `tokens` удалены, линейного поиска больше нет. Поведение: ошибки значений/операторов получают позицию токена поля (проверено 7 кейсами, включая поле не в позиции 0: `priority = "high" AND   due <= junk` → pos=24 — точная позиция `due`; слово-значение, совпадающее с именем поля, позицию не сбивает). |

## Полная регрессия (лично запущено)

- `smoke_7_2.py` → **ALL PASS (43/43)** — было 32, dev добавил 11 (битый JSON,
  дубликаты всех полей, диапазон due, 401-битый-json); подсчет PASS-строк 43.
- `smoke_7_1.py` → **ALL PASS (20/20)** — GET-поиск не сломан.
- Независимый смоук ревьюера (вне smoke-файлов dev): **49 проверок PASS** —
  перечислены выше + регрессионный блок: sdd-пример
  `priority = "high" AND tag IN ("home")` → 200; инъекция
  `category = "work; DROP TABLE tasks;--"` → 200, 0 совпадений, payload только
  в bind-параметрах, таблица `tasks` цела; пустой фильтр → все задачи;
  `normalized_query == build(parse(x))`; GET /api/search жив.
- `openspec validate --all --strict` → 1 passed, 0 failed.
- `flow_check.py` (из ai-factory, на ekotov-wiki) → OK.
- Рабочее дерево чистое, диф исправления — только `search.py` + `smoke_7_2.py`.

## Замечания

Новых замечаний нет (blocker/major/minor — 0).

## Что НЕ проверено

- Браузерный UI (задача 7.3) — вне диффа.
- Реальный uvicorn/HTTP-стек, конкурентная нагрузка, производительность —
  как и в 001, проверка через TestClient.
- Код не правился (независимость ревьюера); smoke-файлы dev не менялись.

## Итог

Задача 7.2 может вливаться. К задачам 7.3+ (UI конструктора) требований
из этих замечаний не вытекает: формат ошибок «duplicate field X» уже
единообразен с остальными 400-ответами `filter syntax: position N: …`.
