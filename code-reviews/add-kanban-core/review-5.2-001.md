# Code Review 5.2 — UI fast line (001)

- **Коммит:** 50fafb5 «task 5.2: fast line UI»
- **Ревьюер:** сабагент code_reviewer (изолированная сессия, auto-edit)
- **Дата:** 2026-09-18
- **Арбитры:** openspec/changes/add-kanban-core/specs/fastline/spec.md, requirements.md FR-3/ОГР-5, sdd.md §3.2/§3.3, tasks.md 5.2

## Вердикт: **approve**

Blocker/major не найдены. Два minor-замечания, не блокирующие вливание.

## Замечания

| # | Файл/место | Серьезность | Замечание | Рекомендация |
|---|---|---|---|---|
| 1 | backend/smoke_5_2_dom.js:4 | minor | Захардкожен абсолютный путь к jsdom (`/home/openclaw/.hermes/hermes-agent/node_modules/jsdom`) — скрипт невоспроизводим вне этой машины. | При появлении package.json / собственного node_modules в проекте перевести на обычный `require("jsdom")`. Сейчас допустимо (скрипт вне прод-пути). |
| 2 | backend/smoke_5_2.py:66–80 | minor | Проверки CSS/JS — assert по подстрокам исходников (например `'classList.toggle("has-fast", hasFast)'`): хрупко к любому рефакторингу, проверяют текст, а не поведение. | Для поведенческой части это уже компенсировано jsdom-смоуком; при рефакторинге допускается править assert-строки, спекой не связаны. |

## Проверенные Scenario (specs/fastline/spec.md)

- **Fast line отображается на доске** (светло-синяя прозрачная подсветка): `.board-column.has-fast` = `rgba(52,152,219,0.1)` + рамка `0.35`; класс ставит board.js при рендере (`classList.toggle("has-fast", hasFast)`). ✅ jsdom-смоук OK.
- **Создание задачи с назначением на fast line**: чекбокс `task-is-fast` в форме создания; форма открыта кликом, создание без fast → закрытие формы. ✅ jsdom-смоук сценарий 3.
- **Негативный: вторая fast-задача — через интерфейс** (ключевой негативный сценарий 5.2): 409 `{error: "fast line occupied"}` → ветка `response.status === 409` в `handleApiError` выводит «fast line занята» в `task-form-error`; `onOk` (закрытие формы + refresh) не вызывается — форма остается открытой, задача НЕ создана. ✅ jsdom-смоук сценарий 2: сообщение показано, overlay не скрыт. Серверная часть 409 — инвариант 5.1, подтвержден TestClient-смоуком (шаг 3).
- **Fast-задача выделяется визуально** (приоритетная): `.task-card-fast` + бейдж fast + `data-fast="true"`; порядок карточек = порядок ответа сервера (sdd §3.3: fast первым, далее по приоритету) — клиентская сортировка не введена, регресс 4.3 отсутствует. ✅ TestClient (шаг 4: fast high → high → medium → low) и jsdom (порядок 2,1,3 сохранен как в ответе).

## Регресс смежных задач

- **4.3 (рендер доски):** `renderBoard` не изменил структуру — только добавлены `hasFast`-сбор и toggle; `COLUMNS` по-прежнему `["todo","in_progress"]`, столбец «Выполнено» карточки не получает → подсветка has-fast на «Выполнено» невозможна ни при каком ответе API (класс не ставится, т.к. статус не итерируется). ✅
- **4.5 (форма):** ветка 409 вставлена до финального fallback в `handleApiError`; 422/401/не-JSON ветки не тронуты; `is_fast` по-прежнему отправляется только при создании (`currentTaskId === null`), PATCH-режим чекбокс скрывает. ✅
- **XSS (ОГР-11):** `innerHTML`/`insertAdjacentHTML`/`document.write` в board.js отсутствуют — весь рендер, включая новые классы, через `createElement`/`textContent`/`classList`; `task-card-fast` ставится из булева `task.is_fast`, не из данных сервера. ✅
- **NFR-7 (auth):** смоук подтвердил редирект `/board` → `/login` без сессии (шаг 1) — не сломан. ✅

## Смоук-скрипты в backend/ (вопрос размещения)

`backend/smoke_5_2.py` и `backend/smoke_5_2_dom.js` размещены рядом с кодом (решение ПМ — допустимо). В прод-путь не входят: `backend/app/` их не импортирует, nginx раздает статику напрямую, приложение статику не маунтит. Смоук использует временную БД (tempfile) — риска задеть продовые данные нет. Замечаний к размещению нет (кроме minor №1).

## Ворота

- `openspec validate --all --strict`: **1 passed, 0 failed** (INFO по auth/spec.md — несущественно для этой задачи).
- `flow_check.py`: **OK**.

## Что НЕ проверено (честно)

- Реальный браузер (Chrome/Firefox): визуальная различимость оттенков (0.1/0.14 альфа) не оценивалась глазами — только корректность применения классов в jsdom и значений в CSS.
- Scenario «Назначение новой fast-задачи после завершения предыдущей» (освобождение линии после «Выполнено») — серверная логика 5.1, к дифу 5.2 не относится; UI-часть покрыта косвенно (сценарий 3 смоука).
- Эдж-кейс «409 при PATCH»: инвариант 5.1 проверяется только при создании, PATCH is_fast не принимает — ветка 409 в JS на редактирование не сработает (fallback «Ошибка запроса (HTTP 409)»), что корректно.

## Решение по вопросу ПМ о цвете

**Принять как соответствие FR-3.** Спека и requirements требуют «светло-синюю прозрачную подсветку» без фиксации точного оттенка; `rgba(52,152,219,…)` = #3498db (насыщенность приглушена альфой 0.1/0.14) — светло-синий прозрачный по любому разумному прочтению. Совпадение с акцентом fast-бейджа (#2980b9, та же гамма) — плюс к целостности UI, а не отклонение. Вносить цвет в sdd не требуется; если при архивации change-пакета захочется зафиксировать значение — уместное место design.md (дельта), не sdd как требование.
