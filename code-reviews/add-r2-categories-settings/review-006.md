# review-006 — задача 2.5, поз. 5.1–5.3 (крестик 40×40, приоритеты цвет+SVG, стиль V3 на всю доску)

- **Change-пакет:** add-r2-categories-settings
- **Ветка:** `feature/add-r2-categories-settings-5` (коммит d7475db, origin)
- **Дифф:** `git diff main...origin/feature/add-r2-categories-settings-5` — 11 файлов, +722/−217
- **Ревьюер:** code_reviewer (изолированная сессия, worktree /tmp/rv-25; параллельный ревьюер — /tmp/rv-24, зоны не пересекались)
- **Дата:** 2026-09-22

## Вердикт: **APPROVE**

Blocker и major отсутствуют. Замечания: 2 minor (см. таблицу).

## Замечания

| # | Серьезность | Файл/место | Замечание | Рекомендация |
|---|---|---|---|---|
| 1 | minor | frontend/static/css/board.css, `.priority-field-pill:has(…:checked)` | Цвет выбранного приоритета в форме реализован пилюлей-оберткой через `:has()` — боковая граница пилюли (`border-color`) при выборе не подсвечивается цветом приоритета, меняются только фон/текст (`rgb(225,208,194)` — border-default — остался в момент замера до transition). Различимость без цвета при этом обеспечена (текст опций «Низкий/Средний/Высокий» + цвет фона), FR-29 выполняется. Дополнительно: `:has()` не поддерживается в устаревших браузерах (baseline 2023) — при отсутствии поддержки пилюля просто останется нейтральной, деградация без потери функции. | Опционально: добавить `border-color` приоритета в те же три правила `:has(...)` для полного соответствия эталону V3 («цветная обводка + светлый фон»). Не блокирует. |
| 2 | minor | frontend/templates/board.html, task-form-overlay | Модальное окно формы не имеет `role="dialog"`/`aria-modal="true"` (в эталоне design/form-style-v3.html атрибуты есть; design/README.md относит их к «задаче dev 2.5», однако дифф их не добавляет). Крестик при этом полностью доступен (aria-label есть), функция не затронута. | Добавить `role="dialog" aria-modal="true" aria-labelledby="task-form-heading"` на `#task-form` в последующей задаче (можно вместе с focus-trap). Не блокирует. |

## Проверенные Scenario (собственный стенд: uvicorn :8092 + http.server :8093 (статика), worktree /tmp/rv-25, SQLite /tmp/rv25.db, seed owner/wife)

| Scenario (дельта specs/tasks) | Как проверено | Результат |
|---|---|---|
| «Крестик закрывает форму» (позиция — правый верхний угол) | playwright `bounding_box()`: крестик 39.8×39.8px (layout-subpixel; CSS-размер ровно 40×40, `width/height:40px` в `.modal-close`), координаты `x+w ≤ modal.x+modal.w`, `y ≈ modal.y` — в правом верхнем углу; скриншот 02-form-open.png | PASS |
| «Нажатие крестика закрывает окно без сохранения» | заполнил «Название» = `rv25-крестик-не-сохранять`, перехват network: после клика по `#task-form-close` — overlay скрыт, POST `/api/tasks` = 0, карточка на доске не появилась | PASS |
| «Приоритеты различимы цветом и иконкой» (карточка) | создал 3 задачи (low/medium/high) через UI-форму; бейджи на карточках: SVG-пути дословно эталона (`M7 2v9M3.5…` low; `M3 5h8M3 9h8` medium; `M7 12V3…` high) + текст «Низкий/Средний/Высокий» + различимые цвета (olive/amber/red); скриншот 05-board-badges.png | PASS |
| «Приоритеты различимы цветом и иконкой» (форма) | select приоритета обернут в `.priority-field-pill`; при выборе low/high/medium фон и цвет текста пилюли меняются на приоритетные токены (мягкие фоны #eff1e4/#f6ebe8/#f4ecdf — как в эталоне V3); текст опций русскими подписями — различимость без цвета | PASS |
| «Иконки приоритетов не требуют внешних библиотек» (ОГР-8) | перехват всех network-запросов страницы: внешних хостов 0; иконки — inline SVG через `createElementNS` (`priority-icons.js`), `innerHTML` не используется (grep по диффу — 0 вхождений); весь CSS/HTML — системные шрифты, без CDN | PASS |
| «Подсветка невалидной категории» (регресс 3.1) | классы `.field-invalid`/`.field-error` сохранены в диффе (`--status-error`/`--status-error-bg`), логика task-form.js не тронута (task-form.js вне диффа, 0 строк) | PASS (смок 3.1, не гонялся отдельно — код не менялся) |
| «Анимация открытия и закрытия формы» | `animationName = modal-in` (220ms, cubic-bezier эталона) при открытии; при `reduced_motion=reduce` — `animationName = none`, окно отображается полностью и мгновенно (скриншот 04); форма закрывается штатно | PASS |

## Обязательные проверки (по заданию)

1. **Соответствие эталону V3** — токены в `app.css :root` дословно совпадают с `design/form-style-v3.html` (терракота `#a8432c`, страницы `#faf7f2`/`#fffdf9`, ink `#3d3630`, Georgia-серифы заголовков через `--font-family-display`, поля с нижней линией 2px, пилюли `border-radius:999px`, focus-ring терракотовый). Compute-стили на живой странице подтверждают (Georgia на h1, bg `rgb(250,247,242)`, accent `#a8432c`). Скриншоты 01/02/05 — визуально V3. ✅
2. **Крестик** — 40×40px CSS (playwright bounding_box 39.8 из-за border-box/subpixel), top-right подтвержден координатами, закрытие без сохранения подтверждено (0 POST, 0 карточек). ✅
3. **Приоритеты** — SVG через `createElementNS`+`setAttribute` (XSS-безопасно, `innerHTML` отсутствует); различимость без цвета = иконка ↓/=/↑ + текст всегда; в форме (пилюля + подписи опций) и на карточках (бейджи). ✅
4. **Доступность** — `:focus-visible` правила на всех контролах (крестик: `box-shadow` терракотовый ring подтвержден на live-странице; nav-item, logout, кнопки форм, поля, чекбокс); `prefers-reduced-motion` → `animation: none` (замер: `animationName=none`); `aria-label="Закрыть форму без сохранения"` на крестике (совпадает с эталоном). ✅
5. **ОГР-8** — grep `http` по всем CSS/HTML диффа: только `http://www.w3.org/2000/svg` (namespace, не сеть); внешних хостов в network-логе страницы — 0; новых зависимостей нет. ✅
6. **Граница** — дифф не содержит `backend/**`, `task-form.js`, `search.js`; `board.html` — только стили/разметка формы и доски (поле тегов `#task-tags` не тронуто, grep диффа по «тег» — 0 изменений). ✅
7. **Эскалация: подписи бейджей vs сырые значения** — разделение оценено как **корректное**: спека 5.2 (дельта tasks «Визуал формы») требует цвет+иконку в форме и на карточке; контракт утвержденного e2e TC-UI-009 требует сырые значения (`high`/`medium` exact) в `#task-detail-attrs` (attrs = значения задачи, FR-9) и `medium` exact на карточке доски. Комментарий в `task-detail.js` фиксирует это решение; примечание: на карточке доски теперь русская подпись «Средний» вместо сырого `medium`, что формально расходится с шагом 7 TC-UI-009 (`get_by_text("medium", exact=True)` на карточке). Однако тест и так падает на main (см. п. 8, предсуществующее падение — категория-селект vs `.fill()`), т.е. расхождение теста с UI не создано этой веткой; вопрос синхронизации TC-UI-009 с новым визуалом — в зону QA-цикла 2.6 (эскалация оркестратору, не return dev). ✅
8. **Ворота** — `openspec validate --all --strict`: 9 passed, 0 failed; `flow_check.py` (/tmp/rv-25 и главное дерево): OK. **Web-регресс = baseline main**: ветка 5 failed/16 passed, main 5 failed/16 passed — списки FAILED идентичны (diff = пусто), все падения предсуществующие: 3 × категория-селект vs `.fill()` (следствие задачи 3.1, попадет в скоуп QA 2.6), 1 × archived-поиск, 1 × datalist `list=`-контракт. Ветка тестов не меняла (0 строк диффа в `tests/`). ✅

## Что проверено дополнительно

- Локальный прогон обоих стендов чистился после ревью (worktree /tmp/rv25-main удален, БД и сервproc остановлены).
- Идентичность финального дерева: один коммит d7475db поверх main, `git status` worktree чистый, файлы диффа = 11 заявленных.
- Эскалаций по границам зон с параллельной сессией /tmp/rv-24 нет: записи только в `code-reviews/.../review-006.md` и /tmp/rv-25.

## Что НЕ проверено (честно)

- Полный `tests/` API-сьют (82 теста) не гонялся — по заданию web-регресс; API-логика в диффе отсутствует (backend не тронут).
- Focus-trap, Esc, клик по подложке, возврат фокуса на триггер — вне скоупа дельты (design/README.md «Вне скоупа мокапов»); не проверялись.
- Реальный закрытие формы крестиком в Firefox/Safari — стенд только chromium.
- Соответствие `aria-*` мокапа (aria-describedby/aria-invalid на ошибке категории) — из кода 3.1, не воспроизводилось на стенде (код не менялся).
