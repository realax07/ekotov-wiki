# Ревью 002: повторное (return-фикс 27de791, review-001)

- **Change-пакет:** `add-r2-categories-settings` (Релиз 2)
- **Объект:** коммит `27de791` (return-фикс 3 minor-замечаний review-001), дифф `git diff aa81df9..27de791`
- **Ветка:** `feature/add-r2-categories-settings-1`
- **Ревьюер:** code_reviewer_agent, изолированная сессия
- **Дата:** 2026-09-22
- **Тип:** повторное ревью — проверяется **только диф-граница фикса**, задача целиком не переоткрывается
- **Затронуто фиксом:** `backend/app/categories.py` (+6/−4), `backend/app/migrate_categories.py` (+19/−6)

---

## Вердикт: **APPROVE**

Все три minor-замечания review-001 устранены в границе диффа, поведение не изменилось и подтверждено прогонами на копии боевой БД.

---

## Проверка каждого finding

| # | Finding review-001 | Фикс в 27de791 | Статус |
|---|---|---|---|
| 1 | `IN_USE_BODY` используется наполовину (только поле `error`) | Константа удалена; тело 409 `{"error":"category in use","details":{"tasks":N}}` собирается целиком в `delete_category` (categories.py:200–207), комментарий ссылается на sdd r2 §3.1 | ✅ устранен |
| 2 | rename: два UPDATE без явного `BEGIN IMMEDIATE` | `conn.execute("BEGIN IMMEDIATE")` перед первым UPDATE (categories.py:160); commit/rollback охватывают оба UPDATE; докстринг обновлен | ✅ устранен |
| 3 | миграция: INSERT категорий и fast-приведение в разных транзакциях | `_insert_categories` открывает `BEGIN IMMEDIATE`, commit убран из обоих шагов и выполняется один раз в `main` (migrate_categories.py:189) после шага 5; докстринги обоих шагов и модуля обновлены | ✅ устранен |

## Граница диффа

`git diff aa81df9..27de791` затрагивает ровно 2 файла (`categories.py`, `migrate_categories.py`), 25+/10−. Все изменения диффа адресуют ровно три finding'а (правки кода + уточняющие докстринги/комментарии). Посторонних правок нет; tasks.md, спеки, tests не тронуты. **Граница соблюдена.**

Техническая проверка корректности фикса №3: соединение из `get_connection()` — sqlite3 с `isolation_level=''` (неявные транзакции); явный `BEGIN IMMEDIATE` выполняется **до** любого DML на соединении (первый DML в `main` — именно INSERT в `_insert_categories`), поэтому ошибки «cannot start a transaction within a transaction» нет — подтверждено прямым прогоном и двумя реальными прогонами миграции.

## Ворота

- `openspec validate --all --strict` → **9 passed, 0 failed**.
- `python3 /home/openclaw/ai-factory/scripts/flow_check.py /home/openclaw/ekotov-wiki` → **OK**.

## Собственные прогоны (факты)

**Окружение:** копия боевой БД `/var/lib/ekotov-wiki/wiki.db` → `/tmp/rv2/wiki.db` (боевая БД не тронута); приложение ветки на `127.0.0.1:8101`, тестовый пользователь в копии БД; после проверок тестовый процесс остановлен (порт свободен).

### Миграция (фикс №3)
- Прогон 1: 6 задач, 3 уникальных непустых category → 3 записи, fast приведено: 1, сверка ОК, exit 0.
- Прогон 2 (идемпотентность): 0 созданных, 0 приведенных, сверка ОК, exit 0.
- **Симуляция краша** между INSERT и UPDATE (os._exit(9) после `_insert_categories`, до commit): в БД не зафиксировано ни одной строки — транзакция атомарна, частично мигрированная БД исключена. Это и есть суть фикса, подтверждена экспериментально.
- Прямой прогон явного `BEGIN IMMEDIATE` на драйвере проекта: конфликта с неявными транзакциями sqlite3 нет.

### API-смоки на копии (13/13 PASS)
- 401 без сессии: GET categories, DELETE category.
- **DELETE используемой → 409, тело дословно** `{"error":"category in use","details":{"tasks":2}}` (фикс №1; категория осталась).
- rename 200; **каскад**: задача после rename несет новое имя (фикс №2, двойной UPDATE в транзакции).
- rename на занятое имя → 409, и категория, и задачи после отката не изменены (атомарность подтверждена с обеих сторон).
- PATCH неизвестной категории → 404.
- 422 ghost на POST и PATCH задач — тело дословно sdd r2 §3.2 `{"error":"validation","details":{"category":"not in categories"}}` (контроль неизменности — код 422 в дифф не входит).
- DELETE свободной → 200 `{"ok":true}`.

### Замечание к моей первой смок-версии (не дефект кода)
Первый смок-скрипт дал ложные FAIL из-за моих же артефактов: expectation тела 422 неверный (правильное тело — из sdd r2 §3.2, подтверждено по openspec-дельте), GET /api/categories/{id} возвращает 405 (маршрута нет — список возвращает только GET /api/categories), и первый curl без `cat -s` поймал 307-редирект на слэш. Все три случая перепроверены корректными запросами — выше приведены только подтвержденные результаты.

## Что НЕ проверено (честно)

- Миграционный systemd-юнит на VPS (вне границы диффа).
- Параллельная запись (WAL) — консервативный `BEGIN IMMEDIATE` только сужает окно; гонки не моделировал (не входят в findings).
- UI-часть (задачи 2.x/3.x) — вне диффа, не переоткрывалась.

## Замечания

Новых замечаний нет. Blocker/major/minor — пусто.
