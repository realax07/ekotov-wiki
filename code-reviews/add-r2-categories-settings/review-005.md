# review-005 — задачи 4.1 + 4.2 (автодополнение тегов + priority-lock)

- **Change-пакет:** add-r2-categories-settings
- **Ветка:** `feature/add-r2-categories-settings-4` (коммит dcc1ee0, origin)
- **Дифф:** `git diff main...origin/feature/add-r2-categories-settings-4` — 4 файла, +105/−4
- **Ревьюер:** code_reviewer (изолированная сессия, worktree /tmp/rv-24)
- **Дата:** 2026-09-22

## Вердикт: **APPROVE**

Blocker и major отсутствуют. Замечания: 2 minor (см. таблицу).

## Замечания

| # | Серьезность | Файл/место | Замечание | Рекомендация |
|---|---|---|---|---|
| 1 | minor | backend/app/tasks.py, PATCH priority-lock | Проверка `bool(row[6])` опирается на позицию `is_fast` в константе `TASK_COLUMNS` (индекс 6) — корректно сегодня, но хрупко при изменении порядка колонок; остальной код диффа ту же информацию получает именованно. Фактически Smoke №5–7 подтверждает корректность. | Вынести выборку строки в именованный кортеж/dict (или локальную константу-индекс `ROW_IS_FAST = 6` рядом с `TASK_COLUMNS`) — в рамках последующей задачи, не блокирует. |
| 2 | minor | frontend/static/js/board/task-form.js, `loadTagHints` | При сбое загрузки подсказок (в т.ч. 401) datalist молча остается пустым — поведение соответствует комментарию и FR-26 (ввод не блокируется), но диагностика сбоя недоступна: сеть/сессия/пустой справочник неотличимы. | Опционально: `console.warn` в error-ветке (без UI-шума). Не блокирует. |

## Обязательные проверки (по заданию) и результаты

### 1. Смок на стенде в /tmp (копия БД, uvicorn 127.0.0.1:8123, seed owner/wife)

Прогон `review_smoke.py` + `review_case4.py` — **17/17 PASS**:

| Случай | Результат |
|---|---|
| POST fast+low | 422 дословно `{"error":"validation","details":{"priority":"fast requires high"}}` ✓ |
| POST fast+medium | 422 дословно ✓ |
| POST fast+high | 201, priority=high, is_fast=true ✓ |
| POST fast без priority (NULL) | 201, priority=high ✓ (проверен отдельно на свободной линии — первый прогон уперся в соседний инвариант 409) |
| PATCH low на fast | 422 дословно ✓ |
| PATCH medium на fast | 422 дословно ✓ |
| **PATCH priority=NULL на fast (dev-обход)** | **422 дословно** ✓ — `body.priority != "high"` при NULL дает 422; обход закрыт |
| PATCH без ключа priority на fast | 200, значения не тронуты (partial update не сломан) ✓ |
| PATCH priority=high на fast | 200 ✓ |
| Обычная задача: POST low | 201 low — не ограничена ✓ |
| Обычная задача: PATCH medium / NULL | 200 / 200 (очистка приоритета обычной задачи допустима) ✓ |
| Соседний инвариант: вторая активная fast | 409 `fast line occupied` — не регрессировал ✓ |

### 2. Дословность тела 422 (sdd r2 §3.2)

Подтверждено побайтово в смоках 1, 2, 5, 6, 7: `{"error":"validation","details":{"priority":"fast requires high"}}` — совпадает с sdd r2 §3.2 и константой `FAST_REQUIRES_HIGH_422`. Оба пути (POST и PATCH) используют одну константу — расхождение невозможно.

### 3. Граница диффа

Ровно 4 файла: `backend/app/tasks.py`, `frontend/static/js/board/task-form.js`, `frontend/templates/board.html` (поле тегов + datalist), `openspec/changes/add-r2-categories-settings/tasks.md` (чекбоксы 4.1/4.2 → [x]). Лишних файлов нет. Зона записи соблюдена.

### 4. Ворота

- `openspec validate --all --strict` → **9 passed, 0 failed** (только INFO-замечания о длине Requirement — предсуществующие).
- `python scripts/flow_check.py /tmp/rv-24` → **OK**.

### 5. Регресс tests/api — дельта vs main = 0

| Прогон | Результат |
|---|---|
| Ветка (dcc1ee0), БД после досеивания QAT-категорий | **68 passed, 22 skipped, 0 failed** |
| main (c67c60a), та же БД, отдельный стенд :8125 | **68 passed, 22 skipped, 0 failed** |

**Дельта = 0.** Первые прогоны на «чистой» копии давали 8 failed + 1 error — все на setup-фикстуры с категориями вне справочника (422 `not in categories`): известная эскалация FR-21 (см. review-003/004), не регресс диффа; после досеивания литералов фикстур в справочник копии — 0 падений на обеих ветках.

## Соответствие спеке (первый круг)

**4.1 (FR-26, Д-3 → tasks: Автодополнение тегов):**
- datalist `task-tag-hints` из существующего `GET /api/suggestions` — эндпоинт не изменен (механизм един с подсказками поиска, sdd r2 §3.2 «без изменений») ✓
- выбор существующего ИЛИ ввод нового: datalist не ограничивает ввод; новое значение сохраняется через `_set_tags` (get-or-create) — проверено в UI: тег `brand-new-rv5` создан через форму и появился в `/api/suggestions` ✓
- перезагрузка подсказок при каждом открытии формы (create и edit) — `loadTagHints()` в `clearTaskForm` и `openEditForm`; проверено в UI: после создания задачи с новым тегом reopened-форма показывает его в подсказках ✓
- негатив 401: без сессии `GET /api/suggestions` → 401, значения не возвращаются ✓ (смоук 16)
- set-семантика/без дублей/сортировка — свойство существующего эндпоинта, покрыта его регрессом (`test_suggestions_*` green) ✓

**4.2 (FR-27, ОГР-10 → fastline MODIFIED):**
- все 7 сценариев дельты fastline, входящие в зону API этой задачи, проверены (см. таблицу смоков); UI-сценарий «Поле приоритета заблокировано» — зона задачи 4.3 (чекбокс не снят), в дифф не входит — корректная нарезка ✓
- отклонение, а не молчаливая правка: fast+low/medium отклоняются 422, задача не создается ✓
- NULL-приоритет при POST fast → 201 high (приведение вызывающим кодом, sdd r2 §3.2) ✓; NULL в PATCH на существующей fast → 422 (инвариант, дельта: «low», «medium» ИЛИ NULL отклоняются) ✓ — найденный dev-обход закрыт корректно
- приведение существующих fast-задач — в миграционном скрипте (`migrate_categories`, шаг 5), вне этого диффа ✓

**Best practices (второй круг):** опции datalist — только через `textContent`/фабрику `el` (XSS закрыт); SQL параметризован; секретов нет (в review-файлах и коде пароли только тестовые seed-значения conftest); ошибки не глотаются (422-пути возвращают дословные тела); поведение не выходит за трассировку.

**Integration (третий круг):** 409 fast-occupied не сломан; частичный PATCH (без ключа priority) не сломан; подсказки поиска (`search.js`) работают на том же эндпоинте без изменений; регресс 68/68 на обеих ветках.

## Что НЕ проверено (честно)

- Задача 4.3 (блокировка поля приоритета в UI) — вне диффа, ее чекбокс не снимался.
- Playwright-набор `tests/web` — не запускался (не входит в обязательный регресс задания; UI-часть 4.1 проверена собственным браузерным смоком).
- Сценарий дельты fastline «Приведение существующих fast-задач» — проверен запуск миграционного скрипта на копии (отчет «0 приведенных» на чистой БД), но не на БД с «грязными» fast-задачами: скрипт вне диффа этой задачи.
- Миграционная сверка NFR-8 — вне диффа (задача 1.3, уже слита).

## Примечание оркестратору

Известная эскалация FR-21 подтверждена третьим ревью подряд (review-003, review-004, review-005): регресс-сьюит требует предварительного досеивания QAT-категорий в справочник копии БД. Задача 6.1 (qa_automation) — фикстуры должны заводить категории сами.
